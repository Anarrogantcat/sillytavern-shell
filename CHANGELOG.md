# SillyTavern Desktop Shell 更新日志

## v2.2.36 (2026-09-25) — card-compat 0.21.2：修「资金纠正不出按钮」（真因：正则不容空格）

### 修复
- 用户实测「功能没生效」：面板只有资金流体检文字、没有「给 X 加 N 现金」按钮
- **根因**：0.21.0 用紧凑正则从补丁文本找 `累计支出` 锚点，而真实补丁是**带空格的 pretty JSON**（`"op": "delta"`）→ 永远命中 0 次 → 落到「只诊断」分支。实测对照：紧凑 JSON 命中 1 次 / pretty JSON 旧正则命中 **0 次**、容忍空格命中 1 次
- **修法**：`moneyCorrection()` 优先吃扩展已解析的 `ops`（不再猜 JSON 格式），文本解析只作兜底并容忍空格

### 夹具
- compat **494/0**（新增夹具 58 共 3 条，用例就是带空格的 pretty JSON；并顺手修回 `no-spend-anchor` 分支）
## v2.2.35 (2026-09-25) — card-compat 0.21.1：资金纠正的安全边界 + 兼容性实测（95 张卡只有 3 张可用）

### 修复
- 资金纠正加单次金额上限（默认 **10 万**，可放宽）：金额解析错/语义误判时不会写入离谱数字
- 目标字段必须是**数字型**（`现金: {元: …}` 这种对象结构不再作为可加目标）；写入前二次校验，不是数字就中止并记 `money-fix-abort`

### 实测（回答「其他卡能不能用」）
- 95 张本机卡：有变量规则 **57** 张 → 有支出类字段 **3** 张 → 同时有现金类字段 **3** 张（本功能可用）
- 支出字段命名极不统一（`累计支出`/`日常消费`/`高端消费`/`消费对象`/`大额消费`/`消费反差` 各 1 张），所以这条推导**只对少数卡成立**；其余有现金字段的 36 张卡**只诊断不出按钮**

### 夹具
- compat **491/0**（新增夹具 57，3 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 67 ・toolbox 16
## v2.2.34 (2026-09-25) — card-compat 0.21.0：资金纠正（不只诊断，能一键补并把可撤销）

### 新增
- **资金纠正**：卡规则里 `user.累计支出_X` 的增量 = X 收到的钱，所以「该给谁加多少」可以推导。命中时面板直接给一行 `给 林婉婷 加 3000 现金（/林婉婷/经济/现金）`，点击（带确认框）即写入，**每次写入前留快照、面板可一键撤销**
- 推导不出来（无累计支出锚点 / 找不到现金路径 / 无变量基线）→ **只诊断不代写**，夹具 56 六条锁住这些分支
- 项目规则新增 **0.3.3「补偿写入」六条准入**：有依据、不猜方向、写前快照、必须可撤销、必须可见、不越界

### 夹具
- compat **488/0**（新增夹具 56，6 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 67 ・toolbox 16
## v2.2.33 (2026-09-25) — card-compat 0.20.0：资金流体检 + 资金账目对账（查清「给了 3000 但现金没变、统计也不对」）

### 新增（都源自你这两条反馈，查的是原始数据不是猜）
- **资金流体检**：正文/输入出现金额（支持 `3000现金` 与 `三千块`）且该楼补丁没有任何「现金/欠款/钱包/资产/余额」路径 → 面板第③组琥珀色提醒「钱动了吗？模型可能漏写」
- **资金账目对账**：对最近 12 层逐层比较「模型打算写的金额」与「账本实际变化」，不一致就报出楼层与差额

### 实测结论（那张卡）
- 你给的 3000：账本记了（#11 累计支出 5000 → 8000），**但林婉婷.现金从头到尾是 500** —— 没有任何一楼写过 `/林婉婷/经济/现金` 的正向 op
- 账本另有 **2500 虚高**，来源锁定在 **#3**：那一楼的 `<UpdateVariable>` 里**只有 Analysis、没有 JSONPatch**，账本却 +2500；而 **#5** 写了 +2500 却没落地（现金 -2500 会变负 → 透支保护跳过整楼）
- 对账功能在真实聊天上跑出的不一致楼层：**只有 #5**（计划 2500 / 实际 0），与逐层手算完全一致

### 夹具
- compat **482/0**（新增夹具 54 共 9 条、夹具 55 共 5 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 67 ・toolbox 16
## v2.2.32 (2026-09-25) — card-compat 0.19.1：面板字号加可读下限（默认调大一档）

### 变更
- `#cc-panel` 根字号改为 `max(13.5px, var(--cc-font, 1em))`：ST 界面字号偏小时，面板不再跟着缩到看不清
- `panelFont` 默认 **1 → 1.1**；面板内写死的 `12px/13px/15px` 与 `.9/.92/.95em` 统一改成相对值
- 实测（ST 根字号 13px + 默认 1.1em）：面板文字全部落在 **14.0–17.2px**，没有低于 13px 的规则
- 只改 `#cc-panel` 作用域，**未触碰卡的状态栏美化**；夹具 53（4 条）锁住这几条不变量

### 夹具
- compat **468/0**（新增夹具 53，4 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 67 ・toolbox 16
## v2.2.31+doc (2026-09-25) — roadmap 收口：删掉伪「待做」，plot-pilot 标注冻结

### 变更
- card-compat 第 1 条拆分：**已落地**部分（写回失败指引 + 合规重算按钮）与**不可行**部分（自动调 MVU 重新处理变量 —— 无公开入口）分开标注，不再挂在「⌛」下让人以为还能做
- plot-pilot 章节顶部加**冻结说明**：按用户指示停在 0.2.2，下方 P1~P3 与 `plot-pilot-plan.md` 的 F1~F11 是候选清单而非当前任务
- 至此 roadmap 里 card-compat 的 P1/P2/P3 **全部有明确状态**（已落地 / 不可行并附证据），不再有含义模糊的待办
## v2.2.31 (2026-09-25) — card-compat 0.19.0：未声明块清理不再误删世界书条目名（多结构块方向收口）

### 修复
- **95 张真实卡全量扫描发现**：78 张卡的正文会写成 `<条目名>设定…</条目名>`（模型回显世界书原文），旧实现把这**条目名**当「未声明块」清理。现在把世界书 `comment` / `keys` 作为条目名名单传入，命中即不清理，并记 `undeclared-kept-as-title` 日志与计数
- 关键安全评估：**有状态栏/面板的卡里，被清理的标签「出现在卡渲染代码里」的数量 = 0** —— 当前清理逻辑没有破坏任何卡的渲染
- 结论：roadmap 的「多结构块支持」**无需特殊分支**（渲染产出的内容不在样本里），真正缺的是「条目名保护」，本轮补上

### 文档
- roadmap 收口：失败可见化（0.16.2 已做）、多结构块（0.19.0 已做并给数据）、「与 MVU 额外解析打通」标注**不可行**并附证据（MVU 公开 API 只有 `getMvuData` / `replaceMvuData` / `parseMessage` / `isDuringExtraAnalysis`，事件只能监听不能触发；要自动触发就得改 MVU 代码，触碰宿主管线）

### 夹具
- compat **464/0**（新增夹具 52，5 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 67 ・toolbox 16
## v2.2.30 (2026-09-25) — card-compat 0.18.0：面板 UI 优化（统计改芯片组 + 趋势改成能看的图）

### 变更
- 顶部统计从「一整行 ｜ 拼接」改成 **4 组芯片**（运行 / 守护 / 清理 / 覆盖与解析）：每项是 `标签+数值` 胶囊，**出问题转警告色**，正常项中性
- 覆盖度趋势从 `▁▃▅ 21%（前 10 轮 18%）` 改成 **能看的竖条图**：最近 16 层，高度=覆盖率、颜色分档（≥80% 绿 / ≥50% 黄 / <50% 红），右侧大号百分比 + 对比箭头；无历史时给提示
- 新样式全部限定 `#cc-panel` 内（不碰卡的状态栏美化）；夹具 51（6 条）锁住结构
- 顺带把一条脆弱断言（检查中文文案字面量）改成检查稳定标识，避免以后再被 UI 重构误伤

### 实测（无头结构校验）
- 用打桩的 ST API 在 Node 里跑扩展：`cc-stats` 渲染 **4 组 / 18 个芯片**，警告芯片 0（正常态），旧「｜」拼接已消失，`cc-trend` 无历史时输出提示文案

### 夹具
- compat **459/0**（新增夹具 51，6 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 67 ・toolbox 16
## v2.2.29 (2026-09-25) — card-compat 0.17.0：面板直接印出符号说明（不再只在对话里解释）

### 新增
- 第③组对照表下方新增**图例行**：`✅ 已写 ｜ ❌ 没写 ｜ ➖ 值没变 ｜ ⚠️ 写了没生效 ｜ ○ 没写·无法核对 ｜ — 未登场·正常`，每项带悬停说明
- 明确写清：**❌ 只代表「模型这轮没写」，不代表卡不兼容**；**⚠️ 才是「写了但存储没变、状态栏不会更新」**
- 夹具 50（4 条）锁住图例：七个键中英各一、六个符号字面齐全、渲染代码存在、样式限定 `#cc-panel` 内

### 夹具
- compat **453/0**（新增夹具 50，4 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 67 ・toolbox 16
## v2.2.28 (2026-09-25) — card-compat 0.16.4：不许覆写卡的状态栏美化（警告 + 样式表白名单夹具）+ AGENTS v5

### 变更（card-compat 0.16.4）
- 依据你补充的「状态栏有自身美化，每个角色卡还不一样」：字号缩放 / 字号下限这两项会覆写 `#chat .mes_text`（本该默认关闭），**打开时面板顶部显示明确警告**
- 新增「夹具 49」把样式表**自有容器白名单**锁死：选择器必须落在 `#cc-panel` / `#cc-var-bar` / `.cc-log-doc` / `.cc-log-head`，且不得出现 `.mes` / `.mes_text` / `body` / `html` / `:root`；同夹具还锁住字号默认值中性、警告文案中英各一、不往 `.mes_text` 注入 HTML

### 文档（AGENTS.md v5）
- 新增 **0.3.2「状态栏的美化属于卡，不许覆写」**：只改数据不改外观；扩展 CSS 必须限定自有容器；覆写型开关默认关闭并加警告；重渲染才是尊重卡的美化

### 夹具
- compat **449/0**（新增夹具 49，5 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 67 ・toolbox 16
## v2.2.27 (2026-09-25) — card-compat 0.16.3（RFC 6902 语义补齐）+ AGENTS v4（状态栏可动）

### 修复 / 新增（card-compat 0.16.3）
- **`add` 打到数组时改为按 RFC 插入**（原先与 `replace` 同义 → 覆盖而不是插入）；越界下标退化为追加，`-` 追加
- **新增 `copy` / `test` 两个 op**：`copy` 深拷贝（源不变）；`test` 值全等校验，失败即中止后续（已应用的 op 不回滚），并记进 `skipped`
- `runVarOps` 返回 `tested` / `aborted`；审计清单里最后一个未闭环的 P2 到此结束

### 文档（AGENTS.md v4）
- 新增 **0.3.1「状态栏可以动」**：写清状态栏由 `stat_data → 卡的正则/脚本 → 楼层 HTML` 渲染而来，所以「动状态栏」= 动数据层。允许：用公开钩子写/修 `stat_data`、修结构块、重渲染、写完核对；禁止：改别的扩展代码来修状态栏（同 ST 本体一样的更新覆盖坑）、直接塞状态栏 DOM、为好看删用户数据键

### 夹具
- compat **444/0**（新增夹具 48，10 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 67 ・toolbox 16
## v2.2.26+doc (2026-09-25) — AGENTS.md v3：修正发包闸门的两类误报

### 修复
- **发包闸门误报**：0.16.2 的提交自检把 `package.json`（只改了 `version`）与 `scripts/compat-logic-test.mjs`（扩展夹具）判成「壳行为变更」。按本意这两类都不该算 —— 前者是升版本号的必要动作，后者属于扩展的测试
- 判据修正：命令改为「只看壳行为」的组合（排除 `scripts/*test*.mjs`），并明确两条例外；`package.json` 命中时再看 diff 是否只动 `version`
- 顺手用结构校验重建了本文件（每个章节标题出现次数必须为 1），避免规则文件出现重复段落

### 说明
- 本轮实际结论不变：**只改扩展，未打 tag、未发版**（2026-09-25 自检为误报，非真实违规）
## v2.2.26 (2026-09-25) — card-compat 0.16.2：硬失败可见化 + 未声明块并进对照表

### 新增
- **硬失败不再只躺在日志里**：`data-missing` / `varfix-invalid` / `yaml-strict-fail` / `block-yaml-issue` / `undeclared-block-stripped` 同类**连续 3 楼**弹一次气泡（10 分钟冷却）；面板新增「连续失败」行实时显示各类别连续次数（≥3 转警告色）
- 判定抽成纯函数 `emptyFailStreak()` / `noteFailure()`（`logic.js`），夹具与运行时同一实现
- **未声明块进对照表**：第③组下方显示「本卡未声明的块（已清理）：标签（N 字）」，按楼层记账（上限 200 楼）

### 夹具
- compat **435/0**（新增夹具 47，10 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 67 ・toolbox 16
## v2.2.25+doc2 (2026-09-25) — AGENTS.md v2：把「本体」明确为 ST 本体，并新增路径台账与适配层规则

### 新增
- **`AGENTS.md` 新增第零章「载体边界：ST 本体不能动」**（本项目根本约束），含：
  - **0.1 路径台账**（实测）：`D:\AI\SillyTavern\SillyTavern\**`（ST 1.18.0 本体，有 `.git`，会被 `git pull` 刷新）与它自带的 `data\**` → **禁止改**；`Data\default-user\**`（扩展与用户数据落点）→ 可增改；`Shell\**`（套壳运行时）→ 不手改；**本仓库自身** → 可改
  - **0.2 禁止清单**：不改/不补丁/不替换 ST 本体任何文件、不禁用其更新流程、不在本体里加"临时适配"（会被下次更新冲掉）、不删本体 `data`
  - **0.3 适配层 = 扩展**：需要 ST 侧能力时只用公开 API 与公开钩子；ST 升级后扩展挂了 → **修扩展发新版**（走在线通道），不得改成去改 ST
  - **0.4 套壳自身边界**：可改清单 + 不可删本体文件/扩展整目录 + 不手改 `Shell\**`
- 特殊项白名单新增第 2 条「**改 ST 本体**」；自检由三问改**四问**（增"本轮有没有碰 ST 本体"）

### 变更
- 依据用户补充说明「ST本体，不能动，原因，后面本体更新一切改动都会报废，使用插件扩展，就不会这样，最多更新下扩展进行适配」；v1 把"本体"写成"套壳仓库自身"，属歧义，已纠正
- 本轮实测自检：`SillyTavern\**` 近 30 分钟**零写入**，写入只落在 `Data\default-user\extensions\card-compat`
- 不改代码、不升版本号、不打 tag（纯文档，依据本文件 1.1 发包闸门）
## v2.2.25+doc (2026-09-25) — 新增项目专属规则 AGENTS.md（不发包默认 / 改动边界 / 汇报精简）

### 新增
- **`AGENTS.md`（项目根，7030 字节）**：把用户口头下达的项目规则固化成可执行条文，覆盖四块：
  - **自主执行边界**：本体源码（`index.js`/`preload.js`/`shell.*`/`lib/**`/`scripts/**` 等）可改；**不许删本体文件、不许换构建/发布配置、不许删扩展目录、不许删用户数据**
  - **发包闸门**：给出一条命令式判据 —— 本轮 diff 未触及 `^(index\.js|preload\.js|shell\.|i18n|webview-preload|prompt|lib/|scripts/|assets/|electron-builder-lite\.json|package\.json|\.github/)` 时，**一律不打 tag、不发安装包**（只改扩展时扩展走在线通道即生效）
  - **特殊项白名单**（封闭 6 条）：发包/打 tag、删或覆盖非本会话产物、结构性改本体、网络代理类、超资源阈值、需求歧义 —— 只有这 6 类才允许提问
  - **汇报精简**：固定「做了什么 + 为什么这样做 + 结果（数据）」，只有命中特殊项才追加「未决」段
  - **自检与修订机制**：每轮三问（问没问非特殊项 / 动没动本体 / 只改扩展时发没发包），违规下一轮内纠正或升级进白名单；规则本身带修订记录

### 变更
- 不改代码、不升版本号、不打 tag（本条属纯文档，依据本文件 1.2 的发包闸门）
## v2.2.25 (2026-09-25) — card-compat 0.16.1：占位值（未登场/未描述）不再误报红叉

### 修复
- 用户实测截图里「陈慧兰.*」一整片 ❌ 属于**误报**：卡的规则用模板组同时管两个角色，而规则本身写明「陈慧兰在剧情天数<7 时保持未登场」，这些字段登场前本就不该更新
- 新增纯函数 `isPlaceholderValue()` / `isPlaceholderAt()`（占位形态只认 未登场/未描述/未触发/未设定/待登场/待描述，**不含「无」**）；`stateDiffFields()` 加 `pending` 桶；面板该列显示中性 `—` 并在摘要行标出「未登场/未描述 N 项」

### 夹具
- compat **426/0**（新增夹具 46）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 67 ・toolbox 16
## v2.2.24 (2026-09-25) — 扩展在线更新加退避重试（治「哈希不符（该源内容可能过期）」）

### 修复
- **实测现象**：套壳里点「检查扩展在线更新」报 `失败: card-compat(校验不符（该源内容可能过期）：哈希不符: CHANGELOG.md; 哈希不符: index.js; 哈希不符: logic.js)`，而同一时刻用 curl 从 raw.githubusercontent 取到的文件与 `index.json` 的 sha1 **完全一致** —— 说明是**源侧瞬时故障**（实测还看到 LICENSE 那个文件被返回 0 字节），单次失败就会让整个更新判断为「源过期」，用户侧表现为拿不到更新
- 修法：`httpGetText` / `httpGetBinary` 改为**同一 URL 重试 2 次（间隔 400ms / 900ms）**的带退避取件，并显式带上 `Accept-Encoding: identity`（避免压缩响应在个别运行时下未被解压，导致 sha1 无谓不符）。重试语义已用注入式假 fetch 验证：前两次失败、第三次成功 → 返回内容；一直失败 → 恰好在第 3 次后抛错

### 变更
- 三个包版本号递增；card-compat 0.16.0 / plot-pilot 0.2.2 内容不变

### 夹具
- 与 v2.2.23 相同：compat 418/0 ・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 67 ・toolbox 16
## v2.2.23 (2026-09-25) — card-compat 0.16.0：「复制本卡诊断」把面板事实收成一段结论

### 新增
- 体检分组新增 **「复制本卡诊断」**：一键产出一段可复制的文本 —— 结论 / 协议 / 能力 / 规则 / Zod 结构 / 本轮判定 / 状态核对 / 守护统计 / 最近动作，末尾自动归纳「它现在在做什么」并给出下一步（启用被禁用的渲染正则、点补应用变量、去 MVU 重演、检查 YAML 等）。纯逻辑函数 `diagnosisReportText()` / `diagnosisActions()`，不碰写数据路径
- `profileOf()` 补 `bookCount` / `disabledViews` 两个诊断字段；面板中英双语同步

### 夹具
- compat **418/0**（新增夹具 45，9 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 67 ・toolbox 16
## v2.2.22 (2026-09-25) — 修 CI：main 被推进时不再把整次发版打掉

### 修复
- **`release.yml` 的清单自愈步骤会在 main 已领先时 `exit 1`，导致整次发版失败**（v2.2.21 的 CI 就是这样挂的：tag 本身没问题，`--check` 也通过，只是一个与发布无关的「回提交」被拒）。现在：`--check` 通过但 main 已被推进 / 无待提交内容 → 打印 warning 后**继续发版**；只有在确实需要推 main 且推送失败时才失败

### 说明
- 起因：我连续补打了 6 个 tag（v2.2.16~v2.2.21）回填缺失的 Release，其中最后一个 tag 的 CI 撞上了「main 已被后续提交推进」这条守卫。这是我的 CI 守卫设计过严 + 补打时序共同造成的，已修

### 夹具
- 与 v2.2.21 相同（本次只改 CI 配置）：compat 409/0 ・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 67 ・toolbox 16
## v2.2.21 (2026-09-25) — plot-pilot 0.2.2：失败兜底抽成纯函数 + 补 DOM 行为夹具

### 变更
- `getFailureFallback` / `fallbackReason` 抽到 `logic.js`（纯函数），`send()` 的失败分支改为调用它们
- plot-pilot 夹具 57 → **66**：补上原先零覆盖的发送失败/超时路径 —— 其中两条**直接调用真实 `sendViaDom`**（注入假 DOM），验证「按钮一直不可用 → 抛错且不点击」「点了但输入框没变化 → 判失败」

### 夹具
- compat 409/0 ・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot **67/67** ・toolbox 16
## v2.2.20 (2026-09-25) — 审计修复收尾：card-compat 0.15.0（MVU 楼层/基线/作用域 + 校验）

### 修复（card-compat 0.15.0）
- `detectVarScope`：精确 type 优先于 `all_variables` 弱信号（卡写 character 变量不再被写进 message）
- `repairYamlStructure`：只剥外层包裹引号，正文引号不再被删掉
- `mvuCanParse`：改用该楼真实数据当基线，并以 `stat_data` 是否真的变化判定（原先空基线 + 命令级错误只 warn → 假 ✅）
- `applyPatchToMvu`：按 `messageId` 读写（原先用忽略楼层的旧别名 → 写回旧楼层会落到当前楼层）
- `chat`/`character` 作用域只写最后一层（原先每次触发重复写 N 遍同一份共享变量）
- `validatePatchBlock`：op 白名单 + `move` 必填 `from` + 拒绝 from/path 互为祖先

### 夹具
- compat **409/0** ・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16
## v2.2.19 (2026-09-25) — 审计反例固化为回归夹具 + 版本同步

### 变更
- **把本轮修掉的 bug 反例固化成断言**（`scripts/compat-logic-test.mjs` 新增「夹具 43」）：385 → **400** 项，覆盖模板组上限、补丁块内方括号说明、YAML 反斜杠、前缀标签、大小写抽取、move 丢值、命令解析嵌套括号 —— 以后回归会立刻红
- `docs/extension-roadmap.md` 的当前版本与落地条目同步到实际（card-compat 0.14.2 / plot-pilot 0.2.1 / 壳 2.2.19）
- 三个包版本号递增（纯夹具与版本变更，**代码无功能性改动**）

### 夹具
- compat **400/0** ・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16
## v2.2.18 (2026-09-25) — 审计修复第③轮：plot-pilot 0.2.0（重复发送 / 谎报已发送 / 配置截断）+ P3 清账

### 修复（plot-pilot 0.2.0）
- API 模式失败后不再无条件模拟点击（可能重复发送，且违背「只用 API」设置）；回退前检查输入框是否已空
- `sendViaDom` 超时不再静默点击并记「已发送」；点击后确认输入框已清空，否则判失败
- `bindText` 写入设置前按 2000 字截断（与 sanitizeConfig 一致），不再先落盘超大文本

### 变更（P3 清账）
- `scripts/ext-index.mjs`：二进制后缀正则的点号以前没转义（`foo-png`/`note-pdf` 会被判成二进制），并补齐 `wasm/exe/dll/7z/svgz`；否则这些文件会被当文本做 CRLF 归一 → sha1 与客户端不一致 → 该扩展在线更新永久失败
- `electron-builder-lite.json`：`directories.output` 从 `../../dist-electron-v3-lite`（仓库外两层）改回仓库内，避免绕过 clean 并踩上 app.asar 文件锁
- `scripts/cf-download-test.mjs`：清理改为幂等并挂到异常/拒绝路径（原先前面的断言抛错会跳过末尾清理，泄漏 20MB+ 临时目录）；`readMeta(...)` 改可选链，缺 meta 时记为失败项而不是 TypeError
- `tavern-scripts/`（旧版「继续按钮」脚本）：**保留**。该目录自带说明「已被 ST 扩展取代、仅作回滚备份」，属于有意的回退资产

### 夹具
- compat 385/0 ・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16
## v2.2.17 (2026-09-25) — 审计修复第②b轮：card-compat 0.14.1（命令解析 + 长聊内存 + 误弹）

### 修复
- **命令式更新解析截断**：`_.set("角色.金钱", Number(基础值))` 旧正则在第一个 `)` 就截断成 `"Number(基础值"`，且 `insert`/`delete`/`move` 不认。改为括号配对 + 顶层逗号切分（实测 8 个命令样例全部正确）
- **长聊天内存持续增长**：`strictChecked`（存整条消息文本）/ `varFixTriedIds` / `frontAlerted` / `applyAlerted` 无上限 → 统一 400 条上限 + 只存内容指纹 + `CHAT_CHANGED` 清空
- **换聊天误弹气泡**：`hardFailStreak` 跨聊天复位

### 夹具
- compat 385/0 ・plot-pilot 57/57（其余同上一轮）
## v2.2.16 (2026-09-25) — 审计修复第②轮：card-compat 0.14.0（模板组上限 + 6 处解析/写入 bug）

### 安全
- **套壳主线程可被一张卡卡死**：card-compat 的 `expandTemplateGroups` 是指数级展开，实测 **8 组 `{a|b|…}`（每组 10 备选）= 1 亿条**（6 组 = 100 万条 / 454ms、7 组 = 210 万条 / 1654ms），而它在 `profileOf()` 里、**打开聊天就会跑**。现在加硬上限 `TPL_EXPAND_CAP = 2000`（实测同样的输入 2ms 返回 2000 条），超限截断并记 `expandStats.truncated`。见 `extensions/card-compat/CHANGELOG.md` 的 0.14.0

### 修复（都在 card-compat 0.14.0，逐条实测见扩展 CHANGELOG）
- 回退写变量用 `replaceVariables` 整体替换 → 静默清空 `display_data`/`delta_data` 等（旧 TavernHelper）；改为先取全量只改 `stat_data`
- `mvuActive()` 不认 `window.parent.Mvu` → 与 MVU 抢写、delta 二次累加；改为复用 `mvuApi()`
- JSONPatch `move` 先删源再验目标 → 目标不可建时源值丢失；改为先建后删
- `parsePatchOps` 取首个 `[` 到末个 `]` → 块内方括号说明（`（说明：[已更新]）`、`分析[1]:`）让整段补丁被丢弃；改为括号配对 + 优先取对象数组
- `guardBlockYaml` 加双引号不转义反斜杠 → `C:\new` 被 YAML 解析成换行；反斜杠一起转义
- 前缀标签：`<Update` 命中 `<UpdateVariable>`、`</Status` 破坏 `</StatusBar>`；改词边界
- 变量块抽取大小写敏感（`<updatevariable>` 协议算 MVU 却抽不到）；两处改大小写不敏感

### 变更
- 本轮**只改扩展，壳代码未动**；壳版本号 +1 是项目惯例（每次内容更新都留痕）。扩展走自己的在线更新通道：推 `main` + 重建 `extensions/index.json` 后，已装用户下次启动即比对到 0.14.0

### 夹具
- compat 385/0 ・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16（全绿）；本轮为 7 个新反例各做了独立探针验证，尚未固化成夹具（列入第 ④ 轮）
## v2.2.15 (2026-09-25) — 安全补测：`dataRoot` 与 `serverPath` 走同一套校验

### 安全
- **`settings:save` 能写入任意 `dataRoot`**（审计发现，同批里唯一漏掉的一处）：`serverPath` 有 `isUnsafeRmPath` 拦截，`dataRoot` 却没有 —— 而启动时的数据迁移与「首次安装清理」都会对它做 `cpSync` / `rmSync`。现在新增 `isUnsafeDataRoot()`：只接受**绝对路径**，且拒绝盘符根、壳/安装目录、ST 目录，以及它们的父目录；`settings:save` 直接返回错误，启动时若发现 settings 里存的旧值不合法则回退默认值并记日志
- **首次安装清理的反向守卫**：原实现只处理「`dataRoot` 在 ST 目录里」（先搬到临时目录再删），没处理反过来的「`dataRoot` 是 ST 的父目录」—— 那种情况下 `rm -rf <ST>` 会连用户数据一起波及。现在直接抛错并提示改安装目录，不清

### 夹具
- 与 v2.2.14 相同（未改扩展逻辑）：compat 385/0 ・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16
## v2.2.14 (2026-09-25) — 审计修复第①轮：三条 P1（不可逆数据风险）+ 发布链四处硬闸门

### 安全
- **渲染层 DOM XSS（可升级为任意命令执行）**：`shell.js` 的 `renderChangelogMd()` 原先把 CHANGELOG 文本直接拼成 HTML 再塞进 `innerHTML`，`script-src 'self'` 的 CSP 挡不住内联事件处理器，而渲染层通过 `preload.js` 握着 `terminal.exec`（`index.js` 里以 `exec(cmd,{cwd:sillyTavernRoot})` 直执行）。改为**逐行 `escapeHtml` 后再包标签**；顺带修掉空行导致的列表断裂（原实现把连续列表拆成多个 `<ul>`）。已用真实 CHANGELOG + 带 `onerror` 的恶意样例端到端验证：输出不含任何可执行属性
- **回滚安装的版本号可夹带路径穿越**：`lib/tools-app.js` 的 `rollbackInstall(version)` 直接把版本号拼进文件名，`..\..\..\Users\...\evil` 这类值能越出 `rollbackDir`，随后 `spawn(f, ['/S', ...])` 会以 NSIS 静默参数**执行任意 exe**。新增 `safeVersion()` 白名单（只接受 `x.y.z` 数字段），`saveRollbackPackage()` 走同一校验；探针复现的越界路径现已拒绝

### 修复
- **备份变成「假备份」**：开了 zip 压缩时，原实现不管 PowerShell 退出码就 `rmSync(dest)` 删掉未压缩目录 —— `Compress-Archive` 在路径含空格/引号、或 PS 不可用时失败，用户会得到一个有名无实的备份。现在**先校验 zip 真的产出（退出码 0 且文件非空）**，成功才删原目录；失败则保留未压缩目录并在终端提示。保留策略同时收紧：只把「完整备份」计数，未压缩兜底目录不会被当成两份而被误删
- **`afterPack` 对 ST 打包不完整只打 WARNING**：full 包缺 `staging/sillytavern`、或拷完少了 `server.js`/`node_modules` 时，构建照样绿灯，用户装完才发现打不开。改为**直接 throw**，让构建红掉
- **`update:check` 的版本比较把预发布当正式版**：原实现按「段 ×100 累加」，`1.18.0-beta` 与 `1.18.0` 结果相同 → 不提示更新。改为逐段比较，预发布按「低于同号正式版」处理

### 变更
- **NSIS 安装器三处加固（`scripts/installer.nsh`）**：
  - 静默升级分支从来没生效过 —— 旧代码取命令行前 4 字符判 `_?=`，而 electron-builder 传的是 `/S /KEEP_APP_DATA ... --updated _?=<dir>`，前 4 字符恒为 `/S /`。改为 `${If} ${Silent} → Goto Done`：**静默卸载/升级绝不弹框、绝不删数据**
  - 目录归一（补 `\Shell`）从只由 `.onVerifyInstDir` 触发的回调，改成在 `customInstall` 里**幂等执行**，静默安装（`/S`）不再落到 `...\Programs\SillyTavern` 并让「Shell / SillyTavern / Data」三分离塌陷
  - 安装时写 **`.shell-owned` 归属标记**（Data、resources、移出去的 SillyTavern 各一份）；卸载时**只删有标记的目录**，没有标记的（例如用户自己装的 `D:\Games\SillyTavern`）会被保留并打印原因；`xcopy` 内置 ST 之前同样先查标记，避免覆盖合并用户自有 ST。卸载勾选框文案同步说明「还会删套壳设置与已保存的回滚包」
- **发布链四处硬闸门（`.github/workflows/release.yml`）**：新增「版本一致性硬校验」（tag ≠ `package.json` 的 version 直接失败）；缺产物不再 `SKIP` 而是 `Write-Error + exit 1`；自愈步骤给 `git commit` 补退出码检查、推送前要求「HEAD 就是 main 顶端，或恰好是 main 之上那一个自愈提交」、推送后用 `ls-remote` 复核远端哈希；工作流加 `concurrency: release`，并给 `gh` 相关步骤补原生命令错误策略（原来 `gh release edit` 失败会被吞）
- 夹具步骤补上漏掉的 `toolbox-test.mjs`；`UI snapshot check` 去掉 `continue-on-error`（不再用绿灯掩盖 UI 回归）
- `README` 的版本号规则不再写死「当前版本」，一律指向 `package.json`（上一轮就是这里漂移了一版）

### 实测（NSIS 安装器，makensis 3.10 编译 + 静默跑）
- 装了 NSIS 3.10 编译测试台（含 `scripts/installer.nsh` 本体 + 复刻 electron-builder 卸载器的 `un.onInit`/`Section` 插入点），覆盖 4 个场景：静默安装 / 静默升级（旧卸载器 `/S /KEEP_APP_DATA --updated _?=<dir>` 后覆盖安装）/ 静默卸载 / 预置用户自有 ST
- **测出并修掉两个真 bug**（静态审阅看不出来、只有编译+跑才会暴露）：
  - `FileOpen` 不会自动建目录 —— 覆盖安装记录与 `.shell-owned` 标记原本写不进去，且失败是静默的。已改为先 `CreateDirectory` 并检查 `${Errors}`，写不进去时明确提示且**保留数据**（更安全的一侧）
  - 内置 ST 的搬运目标写成了 `$INSTDIR\..\SillyTavern`（多嵌一层），与运行时的 `defaultST = <父>\SillyTavern` 不一致，导致守卫查错路径 → **可能覆盖用户自装 ST**。已改为 `$INSTDIR\..`，守卫与标记路径同步对齐
- 另一处必须改的：守卫原来用 `MessageBox` 提示目录冲突 —— 实测**在静默安装下仍会弹出到真实桌面**（阻塞自动化安装）。已改成 `DetailPrint` 诊断，不弹窗、不阻塞
- 实测通过：静默安装三分离（`Shell` / `SillyTavern` / `Data` 各就各位，壳落在 `<父>\SillyTavern\Shell`）・内置 ST 搬运与标记写入・重复安装不覆盖用户自有 ST（内容与文件都保持原样）・静默升级后 `Data` 仍在
- **未实机验证**（诚实标注）：交互式卸载（勾选框 + NSIS 对话框，需人工点击，无法自动化）与「旧版本卸载器」的真实二进制路径。这两条只做了代码级复核；下次发版时建议在真实桌面上手点一次卸载确认

### 夹具
- 与 v2.2.13 相同的 7 个夹具（本次未改扩展逻辑）：compat 385/0 ・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16
## v2.2.13 (2026-09-25) — 文档事实校准：版本号规则改为现行规则 + card-compat README 与代码对齐（无功能改动）

### 修复
- **README「版本号规则」写的是旧规则**：原文写「第三位最多到 6 / 满 6 进位」，与现行规则矛盾（现行：第三位无上限、唯一例外是第二位为 36 时最多到 6、封顶点 `X.36.6`）。这直接误导下一次发版的进位判断，本次按现行规则改写，并注明「满 6 进位」是旧规则产物
- **README 里「当前 `1.36.x`」过期约 40 个版本**，改为实测的单一来源值 `2.2.12`
- **`extensions/card-compat/README.md` 的版本号与能力表停在 v0.2.8**（`manifest.json` / `logic.js` 已是 **0.13.2**）：标题改为 0.13.2；能力表补上 0.9～0.13 已经落地的部分 —— 补丁生效自检、状态级核对（文本写了 ≠ 变量真变了）、MVU 缺失时的独立兜底引擎、按 `[InitVar]` + 逐楼补丁的幂等重算（只修没生效的楼层）、卡的 Zod 结构静态解析与按结构强制写入、收支保护（负数拦截）、变量块兜底与渲染联动；「对外钩子」扩到 `window.CardCompat` 现有全部成员
- `docs/extension-roadmap.md`：状态行标注撰写版本（card-compat 0.2.7 / plot-pilot 0.1.2）与当前版本（0.13.2 / 0.1.5），「三、版本节奏」的规则同步为现行规则；`docs/plot-pilot-plan.md` 现状行 0.1.2 / 壳 2.0.4 → 0.1.5 / 壳 2.2.13、阶段版本映射与「patch ≤ 6」表述一并校准
- `CHANGELOG.md` v2.2.0 条目下补一行 2026-09-25 注记：当时的「满 6 进位」按旧规则记录，不代表现行规则

### 变更
- 文档校准**不新增能力、不改代码**：套壳 patch `2.2.12 → 2.2.13`；两个扩展的 `manifest.json` / `logic.js` 版本号**不动**（0.13.2 / 0.1.5），只是把 `extensions/card-compat/README.md` 的文字对齐 → `extensions/index.json` 会因 README 哈希变化而重建（CI 已有「自愈」步骤），但**清单版本不变，所以不会触发已装用户的扩展在线更新**

### 夹具
- 与 v2.2.12 相同（本次零代码改动）：compat 385/0 ・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.2.12 (2026-09-25) — card-compat 0.13.2：修「切换角色卡后面板仍显示上一张卡的表」

### 修复
- 你截图里的现象：切到 `Science Worship 20260718` 后，面板第③组仍是「破产后姐姐…」的字段表（`系统.日期/时间/地点…`）。根因：`lastReport` 只在「本卡有规则 **且** 本楼有 `<UpdateVariable>`」时才重建，新卡不满足条件就一直沿用旧表（摘要行会刷新、对照表不会 —— 已实机复现：charId 75→3，摘要行变成「夹取 0 / 类型 13」，表里还是旧卡那 5 行）
- 抽取 `buildReport()`：**不管有没有变量块都建表**；切卡/切聊天先清空再按当前卡重建；新增 `watchCard()`（以「角色 id + 头像」为标识，挂在 `CHAT_CHANGED` / `MESSAGE_RECEIVED` / `CHARACTER_MESSAGE_RENDERED` + 3 秒看门狗，因为 ST 切角色不一定发 CHAT_CHANGED）
- 报表头新增「本卡：<卡名>（第 N 楼）」；本卡无可解析规则时给出提示；顺带修同一路径重复两行的问题（渲染时去重，统计口径不变）

### 夹具
- compat **381 → 385** ・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.2.11 (2026-09-25) — card-compat 0.13.1：无法离线校验的约束「写时提示」

### 变更
- 选定方案 A（不执行卡的任意 JS）：静态覆盖 + 如实报告。本版把「如实报告」补到**写变量那一刻**——修复真的写了楼层、而本卡又有 `.transform()` / `.refine()` 这类静态解析不了的约束时，写日志并弹一次气泡「本卡有 N 处无法离线校验的约束（transform 3、refine 1），本次只应用了可静态校验的部分」
- 「补应用变量」的确认框同样带上这句，按确定前就能看到前提
- 新增 `unverKinds()` 统一归纳措辞（面板摘要行与写时提示共用）

### 夹具
- compat **380 → 381** ・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.2.10 (2026-09-25) — card-compat 0.13.0：Zod 结构解析 v2（夹取真正生效）+ 按结构强制写入

### 修复
- **夹取规则此前对这张卡从未生效**：旧解析器处理 `}).prefault({})` 时多弹一次栈，`林婉婷/陈慧兰` 被弹掉，4 处 `_.clamp` 只剩裸路径 `关系态度`；路径匹配要求长度相等 → 永远不命中。现在实测 4 条 clamp 路径完整（`林婉婷.关系态度` / `林婉婷.堕落进度` / `陈慧兰.关系态度` / `陈慧兰.堕落进度`）
- **按卡的 Zod 结构强制写入**（新开关「Zod 结构校验」默认开）：类型（区分 `z.string()` 与 `z.coerce.string()`）、枚举、夹取 + 范围取交集、整数/取整、`.catch` 兜底；不合规就跳过并在面板与日志列出，不写脏值
- 只在 schema 声明为对象时才允许建中间层；`prefault` 默认值按 zod 语义补齐（只补 `undefined`，不覆盖已有值）

### 新增
- 面板第④组「卡的 Zod 结构」摘要行（夹取/范围/类型/枚举/默认值/对象/整数/catch/取整，以及「无法离线校验的约束」计数）
- 工具 `scripts/schema-coverage.mjs`：用本地角色卡语料实测静态覆盖率 —— 完全可静态校验 **22/51 → 33/51**
- 解析新增：helper 引用 / 非 object 别名内联 / `z.record(z.enum(...))` / `z.array` 元素 / `z.enum`·`union`·`literal` / `prefault`·`default`·`??` / `.min`·`.max`·`.int()` / `.catch()` / transform 白名单

### 夹具
- compat **336 → 380**（新增夹具 40 真实卡回归 / 41 结构强制 / 42 语料补的规则）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.2.9 (2026-09-25) — card-compat 0.12.0：收支保护（金额不再被扣成负数）+ 坏值自愈

### 修复
- **金额被扣成负数**：你那张卡第 5 楼的补丁写了 `delta 现金 -2500`，而当时现金只有 500（第 3 楼已经记过一次支出）→ 忠实应用会得到 现金 -2000 / 累计支出 5000。现在加了**收支保护**：某楼补丁会把「初值非负」的数值扣成负数时，判定该楼算术与真实余额不自洽 → **整楼 delta 不应用**，只应用 replace；拦下的路径写日志 + 弹提示（开关「收支保护」默认开）
- **已经写坏的楼层自愈**：实测把你那局第 5、7 楼的 现金 -2000 / 累计支出 5000 修回 **现金 500 / 累计 2500**，时间和地点保持 14:25 / user家客厅，修完再跑 0 写入
- **卡的世界书**：`[mvu_update]变量更新规则`（uid 11）追加「金额字段规则」（金额只用 replace 写结算后的明确数字、每笔只记一次、现金不允许为负、余额不足写 0 并记欠款）；原文件已备份 `破产后姐姐和美母和我的性交易.json.bak-2026-09-23T20-54-40-630Z`

### 新增
- 开关「收支保护」（`overdraftGuard`）；`CardCompat.recompute()` 返回 `guardHits` / `negFixed`

### 夹具
- compat **336/0**（新增夹具 39，13 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.2.8 (2026-09-25) — card-compat 0.11.0：只修没生效的楼层 + 状态级核对（真解决「时间和地点没更新 / 检查不出来」）

### 修复
- **「时间和地点根本就没更新」**：离线读你的聊天文件确认 —— 第 5、7 楼的 `<JSONPatch>` 合法（写的是 时间 14:25、地点 user家客厅）却**没被 MVU 应用**，各楼 `stat_data` 一直停在 14:15 / user家门口。现在 card-compat **只对没生效的楼层补应用**：以 MVU 的真实值为基线，已生效的楼层一律不动，`delta` 只加一次（幂等，修完再跑 0 写入）
  - 之所以不无脑从 `[InitVar]` 全量重放：第 3 楼没有补丁，但 MVU 自己的「时间流逝」把时间推到了 14:15，全量重放会把它回退成 14:00 —— 那是写坏数据
- **「检查不出来」修好了**：新增**状态级核对**第三列。旧对照表只看回复文本里有没有这条路径，补丁没写进变量时照样打 ✅；现在区分「值真的变了 ✅ / 写了但没变 ⚠️ / 写的值本来就一样 ➖ / 本轮没写 ○」，拿不到 `stat_data` 就直接说无法核对
- **「点击补应用变量没反应」**：0.10.0 的按钮调用了 ST 页面里并不存在的 `showConfirm` 全局函数，异常被吞掉 → 点了没反应。现在点一下先试算：没东西可修就直接提示「所有楼层的补丁都已生效」，要修才弹确认框；异常一律可见

### 新增
- 对齐 MVU 的 `z.coerce` + `_.clamp`：从卡的 Zod 源码抠出数值区间与类型，超界自动夹取
- 作用域探测：模板读 `message` / `chat` / `character` 变量，就写到对应位置（默认与 MVU 一致的 message）
- 新开关 `varRepair`（默认开，检测到补丁没生效会自动修）；「补应用变量」按钮改为只修没生效的楼层

### 夹具
- compat **323/0**（新增夹具 36/37/38，41 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.2.7 (2026-09-24) — card-compat 0.10.0：变量兜底引擎（不依赖 MVU）+ 输入框按钮

### 新增
- **A 自动兜底**：检测不到 MVU 时，card-compat 自己从卡的 `[InitVar]` 建初值、逐楼应用 `<JSONPatch>` / `_.set(...)`、写回变量并重渲染 —— **状态栏不再依赖 MVU**（MVU 在场就完全不接管，避免二次应用）
- **B 手动补应用**：输入框上方新增「补应用变量」按钮（与「剧情推进器」同款按钮条）：把当前楼没生效的补丁补应用一次；MVU 在场时会先确认 `delta` 二次累加风险
- 引擎实测：对「破产后姐姐和美母和我的性交易」的真实补丁，算出的状态与补丁意图完全一致
- 两个开关：`varAuto`（无 MVU 时自动兜底）・`varBar`（显示按钮）

### 夹具
- compat **282/0**（新增夹具 35，12 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.2.6 (2026-09-24) — card-compat 0.9.4：补丁生效自检

### 新增
- 面板第①组新增「本轮补丁」结果行：对比该楼与上一楼的 `stat_data`，直接判断「补丁有没有被 MVU 应用」
  - 五档：已生效 / **补丁没生效**（提示去 MVU 面板点「重演楼层」或刷新页面）/ 有变量块但没有补丁操作 / 没输出变量块 / 拿不到 stat_data 无法判断
  - 实测你那局「破产后姐姐和美母和我的性交易」：第 7 楼补丁合法但**未生效**、第 3 楼**只有 Analysis 没有补丁** —— 这两种现在都会在面板里直接显示
- 生成结束后自动查（2.5 秒静默 + 7 秒提示），「检查」按钮也会跑

### 夹具
- compat **270/0**（新增夹具 34，9 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.2.5 (2026-09-24) — card-compat 0.9.3：修「必更字段」清单（裸 {A|B} 组 + 分组轮询）

### 修复
- **实测「破产后姐姐和美母和我的性交易」状态栏部分数据不更新**，根因之一是 card-compat 的提醒清单：
  - 该卡 17/32 条规则用**不带 `$`** 的 `{林婉婷|陈慧兰}` 写法 → 旧实现不展开 → 提醒里出现 MVU 里不存在的伪路径、覆盖度假阴性
  - 提醒只取**前 12 条**（该卡共 49 条）→ 前 12 条全是「系统+林婉婷基础」，`互动次数` / `身体状态` / `user.累计支出` 永远不出现 → 模型每轮都漏
  - 现在：裸 `{A|B}` 也展开；新增 `pickReminderFields()` 按**顶层分组轮询**取 14 条，实测覆盖全部 6 个分组
- 另附**世界书补丁**（你那台机器上已打，原文件已备份）：给规则条目补了「每轮自检」清单（强制每轮输出 `<UpdateVariable>`、点名最容易漏的 5 类字段）

### 夹具
- compat **261/0**（新增夹具 33，8 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.2.4 (2026-09-22) — card-compat 0.9.2：本楼前端块自检

### 新增
- 面板第①组新增「检查前端块」：回答「正则产出的 ```html 前端块到底渲染了没有」—— 四档结论 `ok` / `partial` / `collapse` / `unrendered`，并给出对应处理（刷新页面、切聊天、重生成；或把酒馆助手的「折叠代码块」设为 `disabled`）
- 楼层渲染后 1.5 秒被动自检（每楼只提醒一次），没渲染就写日志 + 提示一次
- 背景：你那张卡的块一直停在代码块状态，实测**不是 card-compat 的改动**（`lastRun.changed=false`、原消息里 0 个围栏），是酒馆助手的渲染时机问题；这个自检把「没渲染」变成面板上可见的状态

### 夹具
- compat **253/0**（新增夹具 32，8 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.2.3 (2026-09-22) — card-compat 0.9.1：哨兵细分「真缺 / 备选」

### 变更
- 0.9.0 的 `disabled-views` 把所有「被禁用的渲染正则」都算预警，实际 14 张里只有 1 张真缺；现在按「是否已有同类启用项」细分：
  - 真缺 → `disabled-views` 预警（实测仅 `Science Worship 20260718`）
  - 旧版 / 移动端 / 备用 / 二选一 → `disabled-alternative`（面板注明「有同类启用项，属备选」，不打扰）
- 新增 `viewNameCore()` / `longestCommonRun()` 与 `disabledUncovered` 计数

### 夹具
- compat **245/0**（新增夹具 31，11 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.2.2 (2026-09-22) — card-compat 0.9.0：新卡哨兵（禁用渲染正则 / 疑似新方言）

### 新增
- **新卡哨兵**（体检）：① `disabled-views` —— 本卡有被禁用的插图/状态栏/面板正则，而卡里没有「自动开启角色卡局部正则」这类 helper（那些东西根本不会显示；实测你库里 **14 张**）；② `new-dialect` —— 卡写了 `check:` 规则却一条都抽不出（= 出现了新写法，给我看的信号；当前库里 0 张）
- 卡自带 helper 会自动开启的**不预警**，只在协议行注明（6 张，含「与继母的丝袜与日常」的 4 条插图正则）
- 新增 `detectDisabledViews()`；总览加「预警 …」，每行加 ❗，复制报告带明细

### 夹具
- compat **234/0**（新增夹具 30，10 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.2.1 (2026-09-22) — card-compat 0.8.1：修 P0 —— 变量补丁被当「未声明块」删掉（新卡兼容性审计）

### 修复
- **P0**：MVU 的标准输出 `<UpdateVariable>…<JSONPatch>[…]</JSONPatch>…</UpdateVariable>` 里，`<JSONPatch>` 在多数卡里并未单独声明 → 被「未声明块清理」整段删掉，**变量更新静默失效**；91 张卡里 74 张会跑守护，**67 张中招**
  - 修法：① 协议内部标签进 `KEEP_BLOCKS`（`jsonpatch` / `updatevariable` / `stat_data`…）；② 成对块清理加「保护区」，声明块内部的子块一律不碰
  - 实测：修前 67 张丢补丁 → **0 张**；清世界书回显的原有能力不受影响（反向断言在夹具里）

### 夹具
- compat **224/0**（新增夹具 29，8 条）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.2.0 (2026-09-21) — card-compat 0.8.0：修「图片丢失」（标签括号错乱）

### 修复
- **实测 bug（你发的截图）**：`与继母的丝袜与日常` 里图片没显示、正文里露出一串 `【NSFW_IMG>她压在我身上/美咲_5.jpg</NSFW_IMG>` —— 那张卡的插图正则要的是 `<NSFW_IMG>…</NSFW_IMG>`，模型把开标签的 `<` 写成了全角 `【`；一个字符之差，图片整块不渲染
- card-compat 0.8.0：新增 `repairBracketTags()`（开关 `fixBracketTags`，默认开），认 `【Tag>` / `[Tag>` / `</Tag】` / `【/Tag>` 与「两边都是方括号」四种形态；只处理本卡声明过的标签，且不做裸 `[Tag]` → `<Tag>`（避免误伤正文方括号）
- 已用你的真实消息回放验证：`【NSFW_IMG>…</NSFW_IMG>` → `<NSFW_IMG>…</NSFW_IMG>`，卡的插图正则恢复匹配，正文长度 3085 → 3085（没有任何删改）

### 变更
- **版本号规则修正**：2.1.7 / 2.1.8 / 2.1.9 超出了「patch 最多到 6」的上限（这是我的失误），本次按规则进位为 **2.2.0**
  - 【2026-09-25 注记】「patch 最多到 6」是当时的旧规则，**不是现行规则**（现行：patch 无上限，仅 `X.36.6` 封顶，见 README「版本号规则」）。这条按历史事实保留，不要再按它进位；2026-09-25 已在 v2.2.13 完成文档校准

### 夹具
- compat **215/0**（新增夹具 28，11 条断言）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.1.9 (2026-09-21) — card-compat 0.7.0：修两种 check 写法被漏掉 + 说清「为什么没有规则」

### 修复
- **拆完 10 张「有变量块但抽不到规则」的卡的结论**：其中 2 张其实写了标准 `check:` 规则，是我的解析器漏了 ——
  - 同行条件 `check: 每次购买道具时变动。`（`蛊`）
  - 行内对象 `小时: { …, check: 跨日时归零联动 }`（`欲妈群`，这张有 84 条规则）
- 另一种漏是**条数上限**：`extractRequiredFields()` 默认只取 10 条 → 覆盖度与必更字段只跟了前 10 个字段；守护链路改为 200 条（结尾提醒仍只列 12 条）

### 新增
- **`classifyNoRules()`**：体检给 `no-rules` 的卡标出原因（散文式 / 命令式 / 只有变量结构 / 规则在 schema 脚本 / 世界书里没有规则），总览・每行・「复制报告」都能看到

### 实测（91 张卡）
- 「抽不到规则」**10 → 8**；有规则 48 → **50**；守护+校验可用 45 → **47**；残留 8 张的原因：散文 4・命令式 1・只有结构 1・schema 1・没有 1

### 夹具
- compat **204/0**（新增夹具 27，17 条断言）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.1.8 (2026-09-21) — card-compat 0.6.1：修「开始新聊天后开局面板消失」

### 修复
- **实测 bug（你发的两张截图）**：`归真纪元` 选「开始新聊天」后开局面板整块消失 —— 那张卡的面板正则 `/^\s*【归真纪元·自定义开局】\s*$/` 用 `$` 锁死了整条消息，而 0.6.0 会给问候语补一行 `<StatusPlaceHolderImpl/>`，`$` 一失配面板就不渲染
- card-compat 0.6.1：新增 `anchoredViewConsuming()` / `regexFromFindRegex()`，识别「整条接管」型前端界面，命中时**完全不改消息**（补锚点、清理未声明块、YAML 修复全部跳过）；普通回复不受影响
- 影响面实测：全库 91 张卡里有 3 张是这种写法，**2 张**（`归真纪元`、`魔法少女们的坏妈妈`）的问候语正好被正则吃掉 —— 0.6.0 会把这两张的开局面板改坏，现已全部保护

### 夹具
- compat **187/0**（新增夹具 26，9 条断言，含「普通回复照样补锚点」的反向断言）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.1.7 (2026-09-21) — card-compat 0.6.0：动态状态栏兼容 + 去掉「显示未署名旧版」

### 修复
- **card-compat 0.6.0**：正则转义的占位符（`/<StatusPlaceHolderImpl\s*\/>/g`）以前抽不出标签 → 锚点「隐形」、体检误判成「只有格式标签」、**AI 忘写占位符时补不上**（新卡「归真纪元」正是这种写法，状态栏会整块消失）
- 修 `<(update(?:variable)?)>` 捕获组写法漏认 → 这类卡的数据块、覆盖度、自动补变量重新生效
- 修标签名混进正则/JS 片段：实测 37 张卡每轮误报「格式标签缺」

### 新增
- **前端界面识别**（动态状态栏 / 交互面板）：面板「变量协议」行与体检结果都会标出；体检新增结论 **`dyn-bar`**，总览与「复制报告」带动态状态栏/面板计数
- **去掉「显示未署名旧版」开关**（你点的「第一条」）：v2.1.1 之前的 Release 已全部转为 GitHub draft（公众匿名访问实测 404），本地回滚列表也不再提供展开入口，界面只列正式版回滚包

### 实测（91 张卡）
- 有锚点 65 → **68** ・有数据块 41 → **56** ・可守护 69 → **74** ・「结论=守护+校验可用」33 → **45** ・「只能守护」28 → **18** ・「只有格式标签」11 → **5** 张 ・垃圾格式标签 37 → **0** 张
- 动态状态栏 **55 张（52 张真动态）** ・交互面板 **39 张** ・体检 91 张 131ms

### 夹具
- compat **178/0**（新增夹具 25：含「模型忘写占位符 → 自动补回」的功能断言）・deploy 37 ・remote 33 ・manage 49 ・cf 13 ・plot-pilot 57 ・toolbox 16

## v2.1.6 (2026-09-21) — card-compat 0.5.0：角色卡兼容性体检（可抽规则 40 → 47）

### 新增
- **card-compat 0.5.0 —— 兼容性体检**（扩展面板第⑥组「兼容性体检」：「开始体检」→ 逐卡列出变量协议 / 锚点数 / 数据块数 / 能抽到的更新规则数 / 结论，可按结论过滤 + 「复制报告」）
  - 结论七档：守护+校验可用 / 只能守护 / 有变量块但抽不到规则 / 协议只读 / 只有格式标签 / 靠酒馆助手脚本渲染 / 纯正文卡
  - 纯本地计算、不联网、不改文件：**你的 90 张卡实测 164ms**（Node 侧 139ms）
  - 新增 `scanCardCompatibility()` / `cardTextOf()`，钩子新增 `window.CardCompat.scan(cards?)`
- **更新规则解析器重写**（缩进栈路径解析）：键里含 `.` `|` `{}` 可识别、`check:` 按就近祖先归属、过滤 META/WRAP 噪声键 —— **实测能抽到规则的角色卡 40 → 47 张**（52%）
- **允许路径补反引号形式**（`角色.字段`），并对 `[(),?$*]` 伪路径加守卫
- **格式标签缺失只提示不补写**：新日志 `format-tag-missing` 与新统计「格式标签缺」

### 线上实测（你的 90 张卡，全部解析成功）
- 能力覆盖：有锚点 **65**（72%）・有数据块 **41**（46%）・有更新规则 **47**（52%）・可写回变量 **54**（60%）・可守护 **69**（77%）
- 结论分布：守护+校验 **33**・只能守护 **28**・纯正文 **11**・只有格式标签 **10**・抽不到规则 **8**
- 变量协议：MVU **54**・无 **20**・YAML 结构块 **16**（YAML 类只读守护，不改宿主变量）
- 夹具：compat **159/0** ・ deploy 37 ・ remote 33 ・ manage 49 ・ cf 13 ・ plot-pilot 57 ・ toolbox 16 —— 全绿

## v2.1.5 (2026-09-21) — 正式版起点与回档过滤 + 依赖兜底（card-compat 0.4.0）

### 新增 / 变更
- **定义正式版起点 `v2.1.1`**（带署名的那一版）：设置 → 通用 → 「版本回滚」默认**只列正式版**，并显示「已隐藏 N 个未署名旧版」+「显示未署名旧版」按钮（回滚包本体不动）
- **设置 tab 顺序**改为「通用 / 工具 / 服务器 / **完整性** / **关于**」（关于移到完整性下面）
- **card-compat 0.4.0**：依赖状态面板（ST / 酒馆助手 / MVU 的版本与可用性）+ **兼容模式**（一键关掉补发事件 / MVU 写回 / 自动补变量，只留纯文本守护）+ **联动自检**（补发事件后没渲染成面板会记「联动未生效」）+ **变量协议识别**（MVU / 任意标签的 JSONPatch / YAML 结构块 / `_.set` / `setvar` 宏，并标注能否写回）+ 面板按协议自适应
- 夹具：compat **135 → 150**；toolbox 16 · cf 13 · plot-pilot 57 · deploy 37 · remote 33 · manage 49 —— 全绿
### 待办（需要你的 GitHub 凭据）
- 把 `v2.1.1` 之前的旧 Release 批量转 **draft** 隐藏（对公众不可见、资产保留）：本机 `gh` 未登录且没有 token，`gh auth login` 后我即可执行

## v2.1.4 (2026-09-21) — 工具箱精简 + cloudflared 改为按需下载（修掉自建检测出的 P0/P1）

### 修复（来自「项目自建」检测）
- **安装包不再夹带 `vendor/cloudflared.exe`（52 MB）**：`package.json` 与 `electron-builder-lite.json` 的 `extraResources` 都已移除
  - 旧问题链：`vendor/` 被 `.gitignore` 忽略 → **全新 clone 无法构建**；而 CI 又没有 st-prep 步骤 → **线上安装包缺这个文件，公网隧道对用户是坏的**
    （实测：已装的 2.1.3 里 `resources/` 只有 4 个文件、没有它；本地带它的构建包比线上大约 13 MB）
  - 现在改为**首次开启隧道时按需下载**到 `%APPDATA%/sillytavern-electron/packs/cloudflared/`：带进度推送、PE 头与体积健全性检查、sha256 记录（下次启动比对，检测损坏/被替换）
- 本地 `npm run *` 在 PowerShell 会被执行策略挡住（`npm.ps1`）→ 文档改注 `npm.cmd`
- `.gitignore` 补 `dist-verify-*`；清掉 506 MB 的旧 `dist-electron-v3-lite/` 与 `compat-profile.json`

### 新增 / 变更
- **工具箱分组按需显示**（设置 → 通用 → 「工具箱显示」）：逐个分组勾选，取消勾选即在工具箱里隐藏；状态存 `toolboxHiddenGroups`，另有「全部显示」
- **「🧩 插件工具」并入「🧩 ST 扩展与插件」**：用户自定义插件不再单独占一个分组，合并成一个区块
- **移除「🧪 兼容与检测」分组**（诊断 / 清理占位符 / 渲染通用状态栏 / 记住新类型）：这些是早年给状态栏打的补丁，现由扩展 **card-compat** 接管；`shell.js` 里对应的 149 行实现一并删除
- 隧道状态栏支持进度显示（如 `⏬ 42%（22/52 MB）`）
- 夹具：toolbox **14 → 16**（新增「兼容与检测已移除」「插件分组已合并」两条）；其余不变（compat 135 · plot-pilot 57 · deploy 37 · remote 33 · manage 49）
- 下载/校验逻辑抽到 `lib/cf-download.js`（纯 node，fetch 由调用方注入）→ 新增夹具 `scripts/cf-download-test.mjs` **13 项**：非 PE 内容拒绝且不留半截文件、进度回调、哈希一致时复用且不再请求、被篡改后自动重下、meta 校验；已加进 CI 与 `all:test`

## v2.1.3 (2026-09-20) — 设置新增「关于」页：更新与更新日志都收进去

### 变更
- 设置面板的 tab 由「通用 / 工具 / 服务器 / 更新 / 完整性」改为 **「通用 / 工具 / 服务器 / 关于 / 完整性」**
- **「更新」页取消，「关于」页统一放**：SillyTavern 版本 / 套壳版本（含「查看更新日志」按钮）/ 作者，以及原先在「更新」页的 **SillyTavern 更新** 与 **套壳更新** 两个区块
- 「关于」页里新增**内联更新日志**（`<details>` 折叠，展开时按需读取并渲染，最多 320px 可滚动），原来的弹窗按钮保留
- 兼容旧值：以前记住过 `settingsTab='update'` 的用户，打开设置落到「通用」而不是空白页
- 夹具：toolbox 14 / compat 135 / plot-pilot 57 / deploy 37 / remote 33 / manage 49 —— 全绿（本次只动套壳 UI）

## v2.1.2 (2026-09-20) — 扩展信息卡收成折叠按钮（card-compat 0.3.3 / plot-pilot 0.1.5）

### 变更
- **扩展信息卡默认折叠**：两个扩展的面板底部只留一行 **ⓘ 扩展信息**，点开才展开名称 / Ver / 查看日志 / 作者 / 许可 / 风险说明；展开状态记进设置（`infoOpen`），下次打开保持
- 夹具：compat **133 → 135**、plot-pilot **56 → 57**（新增折叠/记忆状态的断言）

## v2.1.0 (2026-09-20) — 两个扩展都补上「扩展信息 + 查看日志」（card-compat 0.3.1 / plot-pilot 0.1.3）

### 背景（用户要求）
- 用户要求给两个扩展也写一份「扩展信息 + 更新日志」（版本号 / 查看日志按钮 / 作者 / 免费使用声明 / 风险提示）

### 新增
- **card-compat 0.3.1**：面板顶部扩展信息块（名称 / Ver / 作者 sillytavern-shell / AGPL-3.0 / 项目主页 / 免费使用与风险说明）+ **「查看日志」按钮**
- **plot-pilot 0.1.3**：同样一份扩展信息块 + 「查看日志」
- 日志来源是**扩展目录里随扩展一起部署的 CHANGELOG.md**（离线可用、首次读取后缓存），用 ST 原生 `callGenericPopup` 弹出；`renderChangelogMarkdown()` 先整段转义再白名单替换，日志里的 HTML 不会被执行
- 两个扩展的对外钩子分别新增 `window.CardCompat.changelog()` / `window.PlotPilot.showChangelog()`

### 变更
- 版本进位：壳 **2.0.6 → 2.1.0**（第三位已到上限 6）；card-compat **0.3.0 → 0.3.1**；plot-pilot **0.1.2 → 0.1.3**
- 夹具：compat **121 → 133**、plot-pilot **49 → 56**；其余不变（deploy 37 · remote 33 · manage 49 · toolbox 14）

## v2.0.6 (2026-09-20) — 修卡内 YAML 结构错误导致面板不渲染（扩展 card-compat 0.3.0）

### 背景（用户实测截图）
- 角色卡「天狐3」在聊天里弹出 **「错误详情：YAML格式错误: bad indentation of a mapping entry (6:9)」**，状态栏面板整块渲染不出

### 根因（读卡源码 + 聊天数据逐行核对，非推测）
- 模型把变量块写成了两类非法 YAML：
  1. `- 用户: "涂山清璃"` 后面紧跟更深缩进的兄弟键（`行动: …`）→ js-yaml 抛 `bad indentation of a mapping entry`
  2. `穿搭: "长裙堆叠。", 衬衫…` —— 引号闭合后又跟了「, 文字」
- 卡自己的渲染脚本 `createCharacterCard()` 有个硬约束：`userItem['用户']` **必须是对象**，否则整张角色卡 `return null`（卡片消失）——这决定了修复方向

### 修复 / 变更
- card-compat **0.2.9 → 0.3.0**（按「patch ≤ 6」规则进位）：新增 `repairYamlStructure`，把行内标量**下沉**为子映射的 `名字` 键、把引号后的逗号文字并回引号内；兄弟键已有名字类键时只删冗余标量
- 面板第②组新增开关「结构级修复 YAML」（默认开）；统计新增「结构修复 N」
- 夹具 `scripts/compat-logic-test.mjs` **111 → 121 项**（含真实病灶块的 js-yaml 端到端断言与「卡的契约」断言）
- 夹具全绿：compat 121 · ext-deploy 37 · ext-remote 33 · ext-manage 49 · plot-pilot 49 · toolbox 14

## v2.0.5 (2026-09-20) — 修「状态栏要刷新页面才变回面板」（扩展 card-compat 0.2.9）

### 背景（用户实测）
- 用户反馈：AI 回复里出现一整段 HTML/CSS **源码黑代码块**（不是状态栏面板），并且 **「刷新再打开就好了」**

### 根因（逐层读本机文件核实，非推测）
- 那段 HTML **不在消息里**：是「梁丝诺」卡 `regex_scripts[0]「状态栏美化」` 的 `replaceString`（5126 字的 `html 围栏` 模板），渲染时才替换进 `<StatusPlaceHolderImpl/>` —— 所以全数据目录 3688 个文件（含 83 个聊天文件的全部消息与 swipe）都搜不到那段文字
- 酒馆助手（`JS-Slash-Runner/dist/index.js`）：`Vk(content)` 用 `html>` / `<head>` / `<body` 判定「前端内容」；**全量转换只挂在 `chatLoaded` / `MORE_MESSAGES_LOADED`**，运行时只有「单楼增量」（`CHARACTER_MESSAGE_RENDERED` / `MESSAGE_UPDATED` / `MESSAGE_SWIPED`）
- ST（`public/script.js`）：`updateMessageBlock(..., {rerenderMessage:true})` **不发任何事件**（`MESSAGE_UPDATED` 只在手动编辑消息时发）
- ⇒ 本扩展修正正文后重渲染 → 已画好的前端 iframe 被换回源码 `<pre>` → 没人通知酒馆助手 → 停在源码，直到刷新页面（触发全量转换）
- 触发条件由 **2.0.4 引入**：`stripUndeclaredBlocks` 从此真正生效 → 每轮都改动正文 → 每轮都重渲染

### 修复 / 变更
- 扩展 card-compat **0.2.8 → 0.2.9**：新增 `nudgeRender`（默认开：重渲染后补发 `MESSAGE_UPDATED`，面板立刻重画）+ `rerenderOldFloors`（默认关：历史楼层只改文本不重渲染）；面板第⑤组两个开关
- 夹具 `scripts/compat-logic-test.mjs` **102 → 111 项**（新增 9 条防回归断言，含「补发事件名必须是酒馆助手真正监听的那个」）
- 夹具全绿：compat 111 · ext-deploy 37 · ext-remote 33 · ext-manage 49 · plot-pilot 49 · toolbox 14

## v2.0.4 (2026-09-20) — card-compat 0.2.8（P1/P2/P3 全量）+ 扩展管理器面板

### 修复
- **card-compat 0.2.6 / 0.2.7 的「未声明块清理」实际从未生效**：那段代码被误插进 `strictCheckMessage`，里面引用了不存在的 `res` / `changed`，函数一进入就抛 ReferenceError，又被外层 try/catch 吞掉 —— 也就是**模型回显的 <world_setting> 等块其实一直没被删**（0.2.6 的修复只写进了日志期待，没真正跑）
  - 现已放回 `guardMessage` 入口，成为守护第一步（先清理、再 guardText）
  - `scripts/compat-logic-test.mjs` 新增 5 条「入口顺序回归」断言：直接读 `extensions/card-compat/index.js` 源码，断言 `stripUndeclaredBlocks(` 在 `guardMessage` 内、不在 `strictCheckMessage` 内 —— 这类「没报错的空操作」以后会被 CI 拦住

### 新增
- **card-compat 0.2.8**（扩展版本 0.2.7 → 0.2.8）：路线图 P1/P2/P3 全部落地
  - P1 ① MVU 写回兜底（面板按钮 + 失败提示「可点 MVU 面板的重新处理变量」）②「本卡要求 vs 本轮实际」逐字段 ✅/❌ 对照表 ③连续 3 楼缺变量块的气泡提醒（10 分钟去重）④角色卡档案 60 秒缓存 + 手动失效
  - P2 ⑤ 变量路径白名单（`extractAllowedPaths` / `validatePatchPaths`，分「声明命中 / 组内未声明 / 越界」三档，**越界只提示不改写**）⑥覆盖度历史与趋势方块条 ⑦多块记账（`blockPresence` / `extractUpdateBlocks` / `parsePatchOps`，支持一条回复里多个 `<JSONPatch>`）⑧面板拆成五组
  - P3 ⑨ MVU 联动（探测 API + `parseMessage` 只读试解析 + 检测 MVU「额外模型解析」开启时主动让位避免双写 + 对外钩子 `window.CardCompat`）⑩面板中英双语（自动 / 中文 / English）
- **扩展管理器面板**（工具箱 🧩 ST 扩展与插件）
  - 新增 `lib/ext-manage.js`（纯 fs，可单测）+ 主进程 IPC `tools:extManage` + preload `extManage`
  - **列出数据目录里的全部扩展**（不只内置）：版本、来源（套壳内置 / 在线安装 / 用户自装 / 内置但无部署记录）、部署时间、文件数与体积、**是否被手改过**（目录内容哈希对比部署记录，自动排除 `.shell-deployed.json`）、**是否有内置新版本**
  - 操作：更新 / 强制重装（只作用于该扩展）、打开目录、重置界面设置（先备份 `settings.json.shellbak-<时间戳>`）、卸载（**默认移入 extensions/.shell-trash/ 回收站，可还原**）、彻底删除（二次确认，不可还原）、回收站还原
  - `lib/ext-deploy.js` 的 `planDeploy` / `deployExtensions` 新增 `only` 参数（向后兼容，不影响原有调用）
  - `shell.html` 新增管理器区块，`shell.js` 新增 `extManageRefresh` / `extManageAct` 与事件委托，`i18n.js` 补 38 条英文对照
- **CI 补齐此前没在 CI 里跑的夹具**：`compat-logic-test.mjs`（102 项）与 `plot-pilot-test.mjs`（49 项）加入发布前夹具步骤，并新增 `ext-manage-test.mjs`（49 项）
  - `package.json` 新增 `ext:manage:test` / `all:test`，`ext:test` 改为跑 card-compat + plot-pilot + 管理器三套

### 文档
- `docs/extension-delivery.md`（扩展交付与更新机制：内置通道 + 在线通道、部署 7 条规则、换机/重装/别人装套壳各场景、维护者发扩展三步、故障排查表、已知缺口）
- `docs/extension-roadmap.md`（两个扩展的完善方向与优先级、版本节奏与发布流程）
- `docs/plot-pilot-plan.md`（**剧情推进器 v0.2 完整方案**：推进目标可指定 `{step}`、按 `<options>` 选项号推进、快捷键、自动连续推进 N 轮、与 card-compat 联动、按钮位置与英文文案；分 4 个阶段，本次只出方案不实现）
- README 增加「扩展交付与路线图」小节链接

### 说明
- 扩展版本：card-compat **0.2.7 → 0.2.8**；plot-pilot 保持 **0.1.2**（本次只出方案）
- 夹具全绿：compat 102/102 · ext-deploy 37/37 · ext-remote 33/33 · ext-manage 49/49 · plot-pilot 49/49 · toolbox 14/14

## v2.0.3 (2026-09-20) — 插件兜底：模型漏输出变量块时自动补一次并写回 MVU（扩展 card-compat 0.2.7）

### 背景（实测数据）
- 用户反馈「好几个角色卡现在都出现这个问题（状态栏数据不更新），之前没有」→ 查清：
  - 库内 **214 条 AI 回复全部由本地 12B 生成**，含 `<UpdateVariable>` 的**只有 30%**；按卡差异极大（sex专员 5/5 有；美艳杀手 / 寻找鬼女 / JK 为 0）
  - 排除项：Ollama 实测 `context_length=131072`、ST `openai_max_context=2000000` → **不是截断**；全库 78 条含变量块消息只有 4 条缺 `<JSONPatch>`（均为老消息）→ **不是清理误删**
  - 结论：本地 12B 对「必须输出 JSONPatch」这类格式契约遵从度低（≈30%）且随机波动 → 状态栏时好时坏
### 新增
- **card-compat 变量块兜底**（默认关，面板可开 + 「立即补当前楼层变量块」按钮）：漏块时专注短提示词静默生成 → 校验 → 追加 → 写回 MVU
- 夹具：compat **65 → 76 项**；plot-pilot 48/48 · ext-remote 33/33 · ext-deploy 37/37 · toolbox 14/14 全绿
### 说明
- 扩展级功能：已装 2.0.2 的套壳重启后也能通过在线通道拿到；本机已同步
- 建议配合「MVU → 更新方式=额外模型解析 + 模型 deepseek-flash + 打开变量更新出错通知」使用（需授权改 settings.json）

## v2.0.2 (2026-09-20) — 剧情推进器新增「任何卡都显示推进节点」开关（扩展 plot-pilot 0.1.2）

### 修复 / 新增
- 用户提问「有些卡为什么只有续写，另外一个没有？」——查清：这是**探测档位**的设计行为，不是故障：
  - 92 张卡实测：**强信号 2 张**（翡翠湾度假岛→剧本控制器、装甲核心→剧情控制器）、**弱信号 49 张**（含你正在测的 列车求生，命中 剧情节点/推进剧情/剧本）、**完全无信号 41 张**（如 破产后姐姐、寻找鬼女、美艳杀手、蛊 除外）
  - 你的配置已是 `showAdvanceWhenUnknown=true`（弱信号也显示），所以差异只来自「有没有任何信号」；无信号的卡按设计只显示「续写」
- 新增全局开关 **「任何卡都显示「推进节点」」**（`showAdvanceAlways`，默认关）：打开后无信号的卡也显示推进按钮（用兜底文案「（推进到下一个剧情节点）」）
  - 优先级：本卡设置（显示/隐藏）> always / unknown 开关 > 探测档位
- **修掉一个真 bug**：`shouldShowAdvance()` 第一行 `if (!det || !det.has) return false;` 会在无探测时短路，使新开关永远无效 → 改为只在完全没传探测结果时短路
- 立即生效的替代做法（不用等更新）：面板 → 本卡设置 → 「推进节点」选「本卡显示」
- 夹具 `plot-pilot-test.mjs` **45 → 48 项**；扩展版本 0.1.1 → **0.1.2**；`extensions/index.json` 同步重建

## v2.0.1 (2026-09-20) — 清理「未声明结构块」：模型回显的世界书原文不再铺满聊天（扩展 card-compat 0.2.6）

### 修复
- **现象**（用户截图 + 多模态核对）：卡「n前脚被我当成飞机杯…美艳杀手」的粉色美化面板**正常渲染**（说明卡没坏），但同一层消息里**整段世界书被模型回显** —— 截图下部那一大片 `城市总览: |` / `地图结构:` / `详细地点:`（还带黑代码块）就是 `<world_setting>` 里的原文
- **根因（全库核查）**：该层消息出现了 `<world_setting>`×2、`<status_block>`、`<konatan_planning~>` 四个块，而它们在**用户的任何角色卡、世界书、预设、扩展里都不存在** —— 模型把注入的世界书原文当成模板回显，又自创了状态/思考块；卡只认自己的 11 个标签（`<正文>`/`<女主A_*>`/`<女主B_*>`），没人渲染也没人隐藏这些块，于是原文裸露
- **修法**：card-compat 新增 `stripUndeclaredBlocks()`（面板开关 `stripUndeclared`，默认开）
  - 只处理**成对**块；标签既不在「本卡声明集合」（卡的正则 findRegex/replaceString + 酒馆助手脚本里出现过的标签，**含中文**）也不在保留名单（预设块 `tucao/current_event/progress/options/htmlcontent…` + 通用 HTML）时删除
  - **只有开标签的块只报告不删**（怕误伤半截 HTML）；统计「清块 N」，日志 `undeclared-block-stripped` / `unclosed-block`
  - 新增 `broadTagsOf()`（宽松标签抽取，支持中文与 `~` 等杂字符），`buildProfile` 因此得到 `rawTags`
- **实测（用户当前那条 8836 字的消息）**：删掉 `konatan_planning~`(1106) + `world_setting`(2901 + 1751) + `status_block`(234) = **-5992 字（-68%）**，8836 → 2844 字
  - 保留：`<正文>`、`<女主A/B_*>` 全部 11 个标签、`<tucao>`、`<options>` ✓
  - **卡的正则清理前后都匹配成功（12 个捕获组），组 1 与组 11 内容完全一致** → 美化面板不受影响
- 生成前提醒新增通用要求「不要把世界书/设定原文回显进正文，也不要输出本卡没声明的结构块」（**刻意不点具体标签名**，避免诱导模型输出）；卡只有格式标签时，提醒会列出「本卡前端要求正文里包含这些标签」

### 变更
- 夹具 `compat-logic-test.mjs` **59 → 65 项**：新增未声明块清理 6 项（删除/保留/预设块与 HTML 保留/不成对只报告/卡声明过的不误删/清理后卡正则仍匹配）
- 扩展版本 0.2.5 → **0.2.6**；`extensions/index.json` 同步重建；已同步到本机 ST 数据目录
- 说明：这是**扩展级**修复，已装 2.0.0 的套壳重启后也会通过「扩展在线更新」拿到；本机刷新 ST 页面时 card-compat 会扫最近 5 层自动清理并重渲染

## v2.0.0 (2026-09-20) — 修「自带库被 .gitignore 吞掉」导致发布中断 + 加固 CI 自愈（扩展 card-compat 0.2.5）

### 修复
- **v1.36.6 的发布被 CI 拦下（47 秒失败）**，日志给出两个真问题：
  1. **扩展自带库根本没进仓库**：仓库根 `.gitignore` 有 `vendor/` 规则，把 `extensions/card-compat/vendor/js-yaml.min.js` 一起忽略了
     → 本地索引算到 **8 个文件**、CI 检出后只有 **6 个** → 清单校验判 STALE（自愈步骤按预期工作，拦住了打包）
     - 修法：目录改名 `vendor/` → **`assets/`**（不再撞规则），加载路径与文档同步更新；扩展版本 0.2.4 → **0.2.5**
  2. **CI 自愈的 `git push` 403**：`remote: Permission ... denied to github-actions[bot]` —— `actions/checkout` 注入的 `http.https://github.com/.extraheader` 盖掉了 URL 里的凭据，且 job 未声明写权限
     - 修法：job 级加 `permissions: contents: write`；push 时显式 `-c "http.https://github.com/.extraheader="` 清掉注入头；**校验 push 退出码**（此前失败也照样打印"已推回"）

### 变更
- **版本号规则推进**：1.36.x 线走完（patch 上限 6、minor 上限 36）→ 本次进入 **2.0.0**，之后 2.0.1 … 2.0.6 → 2.1.0
- 夹具总览：compat 59/0 · ext-remote 33/33 · ext-deploy 37/37 · toolbox 14/14 · plot-pilot 45/45 · index `--check` 通过

### 说明
- 上一版打的 tag `v1.36.6` 因发布失败**没有 Release**（自动更新只读 Release，故无影响）；本次 2.0.0 为正式可更新版本

## v1.36.6 (2026-09-20) — 结构块严格 YAML 校验 + 扩展回滚通道（card-compat 0.2.4）
### 新增
- **扩展「严格 YAML 校验」**：card-compat 自带 `vendor/js-yaml.min.js`（js-yaml 4.3.0，MIT，去 BOM / 去 sourceMappingURL），生成后对角色卡声明的结构块做**真解析** ——
  失败就在面板（新增 `YAML 严格` 计数）与日志（`yaml-strict-fail` + 首条错误）报警，**只报告不改文本**；懒加载（页面已有 jsyaml 就复用，消息里没结构块就不加载）；面板新增「严格校验当前楼层」按钮
- **扩展回滚 / 指定版本通道**：工具箱 🧩 组新增输入框 + 「按指定版本应用」按钮 —— 填 tag 或 commit（如 `v1.36.4`）就从该引用取清单并**强制覆盖**（允许降级）；
  普通「检查扩展在线更新」仍然**只升不降**。实现：`lib/ext-remote.js` 新增 `basesForRef()` / `rollbackTo()`（raw 与 jsDelivr 均支持 `@tag`/`@commit`，引用做白名单校验，非法输入退回 main）
### 变更
- 主进程 `checkExtensionUpdates()` 接受 `{ref, force}`；IPC `tools:extCheck` 透传；终端会打印「已回滚/指定版本：…（刷新 ST 生效）」
- 夹具：`ext-remote-test.mjs` **25 → 33 项**（ref 构造、非法引用、普通通道不降级、回滚强制降级、取不到清单明确失败）；`compat-logic-test.mjs` **54 → 59 项**（严格校验五类）
- 扩展版本 0.2.3 → **0.2.4**；`extensions/index.json` 同步重建（含 vendor 文件）
### 说明
- **这是 1.36.x 线的最后一版**：按版本号规则（patch ≤ 6、minor ≤ 36），下一个版本号将是 **2.0.0**
## v1.36.5 (2026-09-20) — 结构块 YAML 预检 + 第二类自动修复（扩展 card-compat 0.2.3）
### 新增
- **YAML 预检**：新增 `guardBlockYaml()`（行级规则，不依赖 js-yaml），在守护消息时对角色卡声明的结构块做两类修复、一类报警：
  - 值未加引号却含 `: `（冒号+空格）或 ` #`（空格+井号）→ YAML 会当嵌套键/注释，值被截断或整段解析失败 → **自动给值加英文双引号**
  - 值以英文引号开头、以中文引号 `”` 结尾（0.2.2 修的那类）合并进同一入口
  - 引号开了没闭合等无法安全判断的情况 → **只报告**（面板「YAML 疑点」+ 日志 `block-yaml-issue`），不乱改
- `|` / `>` 字面量块内的行、块外正文、已加引号的值：一律不动（夹具守着）
### 变更
- 面板新增两个开关（默认开、可单独关）：「修结构块里的引号错配」「含「: 」「 #」却没加引号的值自动加引号」→ 解决上一版列的「会写回消息文本」的顾虑
- 面板统计新增「加引号」「YAML 疑点」；日志新增 `scalar-quoted` / `block-yaml-issue`
- 夹具 `compat-logic-test.mjs` **44 → 54 项**：新增含 **js-yaml 端到端断言**（裸 `: ` 值修复前解析失败 → 修复后能取出用户列表）、`|` 块跳过、两个开关各自生效等 10 项
- 扩展版本 0.2.2 → **0.2.3**；`extensions/index.json` 同步重建；已同步到本机 ST 数据目录
## v1.36.4 (2026-09-20) — 修「状态栏显示未解析到角色数据」：结构块引号错配（扩展 card-compat 0.2.2）
### 修复
- **现象**（用户截图）：角色卡前端状态栏里「车厢角色实时状态」显示 `未解析到角色数据`，而「行动指令快捷」正常
- **根因（有硬证据）**：模型把 YAML 字符串的结束引号写成了**中文右引号 ”（U+201D）**，例如 `内心: "……契合。”` —— 用 js-yaml 实测该块**解析失败**（`bad indentation of a mapping entry`），卡的前端取不到 `用户列表` 就渲染成「未解析到角色数据」；同会话里引号配对的楼层解析成功、`用户列表` 长度 4
- **修法**：card-compat 新增 `repairSmartQuotes()` —— 只在角色卡声明的结构块内逐行修「英文引号开头 + 中文引号结尾」的标量（双引号 `”`、单引号 `’`）；块外正文、本来配对的行一字不动；面板加「引号修复」计数，日志 `quote-repaired`
- **实测（用户当前会话 8 条 AI 楼层）**：楼层 11、15 由「解析失败」变为「能取到 4 个角色数据」，各修 1 行（字段 `内心`），其余 6 层零改动
- 生成前提醒新增一条：结构块内字符串引号必须用英文半角并配对，降低复发率
- 夹具 `compat-logic-test.mjs` **37 → 44 项**：含用 **js-yaml 对真实故障文本做端到端验证**（修复前解析失败 → 修复后能取出用户列表）、块外正文不动、单引号变体、未声明标签族原样返回
- 扩展版本 0.2.1 → **0.2.2**；`extensions/index.json` 同步重建
### 说明
- 这是**扩展级**修复：已装 1.36.3 的套壳重启后会通过「扩展在线更新」直接拿到 card-compat 0.2.2，不必等整个安装包；本地也已同步到 `Data/default-user/extensions/card-compat`，当前会话刷新即生效
## v1.36.3 (2026-09-20) — 把上一版列出的三个风险全部解决
### 修复 / 加固
- **风险①：忘了跑 `ext-index.mjs` 会卡发版** → 三重兜底：
  - 本地：`prebuild` / `prebuild:full` / `prebuild:lite` 钩子自动重建清单（任何构建前都会刷新）
  - CI：新增「扩展清单自愈」步骤 —— 过期就用仓库内容重建、`git merge-base --is-ancestor` 校验为 fast-forward 后回提交 main；若 main 已领先则明确报错并提示重发，绝不回退 main
  - 修掉 GitHub Actions pwsh 的坑：原生命令非零退出 / 写 stderr 会被当成错误直接终止步骤，导致自愈逻辑根本没机会执行 → 步骤内先放宽 `$ErrorActionPreference` / `$PSNativeCommandUseErrorActionPreference`
- **风险②：CDN 缓存导致拿不到新扩展** → 先实测再用数据决策：
  - 实测响应头：raw\.githubusercontent\.com `max-age=300`（5 分钟）、testingcf\.jsdelivr\.net `s-maxage=43200`（12 小时）、GitHub contents API 匿名已 **403 rate limit**（故弃用）
  - 取源顺序改为 **raw 优先、jsDelivr 兜底**；清单与文件都带 `?t=<时间戳>` 穿透缓存；同一次检查清单与文件共用同一时间戳（保证拿到同一版本）
  - 新增**单扩展多源重试**：某源内容与清单 sha1 不符（缓存陈旧）→ 自动换下一个源；全源都失败才记失败，且**不写盘**（不会把旧内容覆盖成新版本号）
- **风险③：只支持文本文件** → 补齐二进制通道：
  - `ext-index.mjs` 按扩展名识别二进制（png/jpg/webp/ico/字体/音视频/zip 等），清单里标 `bin: true`、按字节哈希（不做 CRLF 归一）
  - 主进程新增 `httpGetBinary()`（`net.fetch` + `arrayBuffer`，30 秒超时）；`applyRemoteUpdates` 走二进制分支；缺 `fetchBinary` 时明确报错而不是写坏文件
- 夹具 `ext-remote-test.mjs` **19 → 25 项**：新增「陈旧源自动换源后仍成功且内容正确」「二进制按字节落地」「缺 fetchBinary 明确报错且不写盘」等
- 夹具总览：toolbox 14/14、ext-deploy 37/37、ext-remote 25/25、plot-pilot 45/45、card-compat 37/0
## v1.36.2 (2026-09-20) — 工具箱合并（15→8 组）+ 扩展在线更新通道（扩展与套壳版本解耦）
### 新增
- **工具箱「🧩 ST 扩展与插件」分组**：`部署/更新内置扩展`（从安装包内的 `extensions/` 同步到 ST 数据目录）、`检查扩展在线更新`（走 CDN）、`启动时自动检查扩展在线更新` 开关 + 结果明细区（三层接线：`shell.html` → `shell.js` → `preload.js` → `index.js`）
- **扩展在线更新通道（版本解耦的关键）**：新增 `lib/ext-remote.js`（拉 `extensions/index.json` → 比版本 → 逐个下载文件并校验 sha1 → 全部通过才写盘）+ `scripts/ext-index.mjs` 生成/校验清单
  - 清单来源双通道：jsDelivr（`testingcf.jsdelivr.net/gh/...@main`）→ 失败退回 `raw.githubusercontent.com`；主进程用 Electron `net.fetch`（跟随系统代理），20 秒超时
  - 行尾归一（CRLF→LF）后再算哈希，避免 Windows 工作区与 CDN 的 LF 内容对不上
- 启动后 6 秒后台自动检查一次在线更新（`settings.extAutoUpdate`，默认开；失败只写终端不打扰用户），有更新时终端绿字提示「刷新 ST 生效」
- 新增夹具 `scripts/ext-remote-test.mjs`（**19 项**：假 fetch 覆盖双 base 回退、全失败、幂等跳过、内容篡改哈希不符拒绝写盘、缺文件不半套写盘、摘要文案、CRLF 归一）
- 新增夹具 `scripts/toolbox-test.mjs`（**14 项**：分组数/名称、控件 id 无丢失无重复、三层接线齐全、旧分组键清理）
### 变更
- **工具箱 15 个分组合并为 8 个**：🗂 数据与导出 / 🔎 检索与统计 / 👁 角色卡与世界书 / 🤖 模型与本地服务 / 🧩 ST 扩展与插件 / 🧪 兼容与检测 / 💬 对话与界面 / 🌐 网络与远程访问；原有 **34 个控件 id 全部保留**，组内用新增的 `.tool-sub` 二级小标题区分（合并前后顺序见 `shell.html`）
- 合并后自动清理 `localStorage` 里已不存在的旧分组键（`toolboxGroupOrder` / `toolboxGroupCollapsed` / `toolboxFavoriteGroups`），避免历史垃圾把顺序/折叠状态带偏
- CI（`.github/workflows/release.yml`）发版前增加「扩展清单新鲜度 + 部署夹具 + 在线更新夹具」校验：清单过期或夹具红就直接失败，不产包
- 夹具总览：toolbox 14/14、ext-deploy 37/37、ext-remote 19/19、plot-pilot 45/45、card-compat 37/0
### 说明
- 扩展更新现在**不需要**跟着套壳发版：改扩展 → `node scripts/ext-index.mjs` → 提交推送 → 老套壳启动即自动比对更新
## v1.36.1 (2026-09-20) — 内置扩展部署加固（静默日志 + 降级保护）+ 确立版本号规则
### 加固
- **P1 启动日志静默**：`deployExtensions()` 新增 `quiet` 选项，套壳启动时用 `quiet: true` —— 全部跳过时**一行都不打**，只在真的安装/更新/失败时输出一行摘要；新增 `buildSummary()` 统一生成摘要（`card-compat 0.2.1 已安装；plot-pilot 0.1.1 已更新` / `均为最新` / `失败：…`）
- **P2 安装器降级保护**：`scripts/ext-install.mjs` 新增 `guardDowngrade()` —— 目标里装的版本**比仓库这份更新**时默认跳过（避免把人手装的更新版按文件覆盖成旧版），日志给出原因；确需覆盖加 `--force`；`syncExtension()` 同步返回 `{blocked, reason}`，CLI 末尾汇总"降级保护跳过 N 个"
### 变更
- **版本号进位规则（用户 2026-09-20 下达，已写入全局 `AGENTS.md` §5.2.1 与本 README）**：
  第三位（patch）最多到 **6**、第二位（minor）最多到 **36**；第三位满 6 → 第二位 +1、第三位归零（`2.0.6` → `2.1.0`）；
  第二位已是 36 且第三位满 6 → 第一位 +1、其余归零（`1.36.6` → `2.0.0`、`2.36.6` → `3.0.0`）；
  每个大版 = 37 条小线 × 7 个 patch，示例 `2.0.0…2.0.6` → `2.1.0…2.1.6` → … → `2.36.0…2.36.6` → `3.0.0`。
  （本条 v1.36.1 发布后按用户两次澄清修订，代码未变，故未新起版本号）
- 夹具 `scripts/ext-deploy-test.mjs` **23 → 34 项**：新增 quiet 模式（无"跳过"日志 / 无动作零日志 / 摘要文案）、降级保护（拦截、force 放行、目标更旧不拦、被拦时不写文件、force 后确实覆盖）等 11 项；实测 **34/34 通过**
- 未改打包清单与部署策略（v1.36.0 的 asar 实测结论不变：第二次部署 `deployed:0, skipped:[skip-same, skip-same]`）
## v1.36.0 (2026-09-20) — 安装包内置 ST 扩展并自动部署（别的用户也能拿到插件）
- 问题（用户提问「我让你写的 st 本体插件，其他用户安装套壳是不是没有？」）：确实是**没有**——`electron-builder-lite.json` 与 `package.json` 的 `files` 列表里都没有 `extensions/`，而且没有任何代码把扩展写进用户数据目录
- 新增 `lib/ext-deploy.js`：内置扩展部署模块（纯 `node:fs`，无 electron 依赖，可单测）
  - 只处理带 `manifest.json` 的内置扩展；只碰内置清单里的 id，用户自己的其它扩展目录一律不动
  - 目标不存在 → 安装；内置版本（manifest version）比目标**更高** → 更新；相等/更旧 → 跳过（不降级、不覆盖用户版本）
  - 目标目录没有 `manifest.json`（不是合规扩展）→ 不碰，只报告
  - 覆盖时**不做镜像同步**：只写自带的文件，不删目标里的其它文件（保护用户改动）
  - 每次部署写 `.shell-deployed.json`（版本 + 内容 sha1 + 时间）留痕，便于排查/回滚
- `index.js` 接入：启动后 1.5 秒自动部署一次；若 ST 数据目录还没初始化（轻量版首启还没跑过 ST）→ 每 15 秒重试，最多 12 次，并在终端打印原因
- 托盘右键新增「部署/更新内置扩展」手动入口
- 打包清单补上 `extensions/`（`electron-builder-lite.json` 与 `package.json` 的 `build.files`）→ 安装包从此自带 card-compat / plot-pilot
- 新增夹具 `scripts/ext-deploy-test.mjs`：**23 项断言全通过**（版本比较、全新安装、幂等、升级、降级保护、不碰未知扩展、无 manifest 目标、数据目录未初始化、planDeploy 只判断不落盘、force）
- 影响：老用户升级到 1.36.0 后，两个插件会被自动补齐/更新；用户自己改过的同版本扩展不会被覆盖（跳过并在终端说明原因）
## v1.35.1 (2026-09-20) — 剧情推进器面板输入框白底修复（扩展 0.1.1）
> **本次发布范围**：v1.32.1 之后累计 **15 个提交**（v1.33.0 → v1.35.1）都在这一个安装包里，含 card-compat 0.1.x→0.2.1 全部修复、通用版继续按钮、以及新扩展「剧情推进器 Plot Pilot」。
- 现象：用户截图反馈面板里「这么多白色」——7 个文本输入框在深色主题下是浏览器默认白底，与 ST 主题不搭
- 原因：新面板用了 `<input type="text">` 但没套 ST 自己的主题类；ST 的输入框样式只作用于 `.text_pole`（`public/style.css`：`background-color: var(--black30a); color: var(--SmartThemeBodyColor); border: 1px solid var(--SmartThemeBorderColor)`），旧面板没文本框所以没暴露这个问题
- 修复：7 个文本输入全部加 `class="text_pole"`；`style.css` 追加兜底规则（主题变量缺失时也用同一组变量，保证深色）
- 扩展版本 0.1.0 → 0.1.1；夹具 46/46 仍全通过；已同步到 `Data/default-user/extensions/plot-pilot/`
## v1.35.0 (2026-09-20) — 「继续按钮」升级为 ST 原生扩展《剧情推进器 Plot Pilot》(0.1.0)
- 背景：原「继续按钮」是酒馆助手脚本，配置存 localStorage（不进 ST 备份）、逻辑与 DOM 混在一起、无法按卡区分，用户要求"写成插件，方便后续扩展和优化更新，并改名"
- 新增仓库管理的 ST 扩展 `extensions/plot-pilot/`（`manifest.json` / `logic.js` / `index.js` / `style.css` / `README.md` / `CHANGELOG.md`），命名 **剧情推进器 Plot Pilot**，按钮改为「▶ 续写 / ⏭ 推进节点」且文案可改
- 分层：`logic.js` 是纯函数（探测 / 文案 / 每卡覆盖 / 配置清洗 / 通道选择 / 旧脚本冲突判定），不依赖 ST・DOM・酒馆助手，可在 Node 里直接跑夹具；`index.js` 只做 ST 对接
- 配置持久化：从 `localStorage["yd_continue_cfg_v2"]` 迁到 `extension_settings["plot-pilot"]`（随 ST 设置保存/备份），旧脚本里改过的文案会**一次性自动搬过来**
- 发送通道三选一：`api`（把文本写进输入框后直接调用 ST 的 `Generate("normal")`，内部会创建用户消息）、`dom`（模拟点击，等发送按钮可用）、`auto`（优先 api，异常自动退回 dom，实测已切到 api）
- 每卡覆盖：两个按钮可逐卡设为 跟随全局 / 显示 / 隐藏，文案也能逐卡覆盖，并有「清除本卡设置」
- 冲突保护：检测到 `window.YDContinue` 或旧 `#yd-quick-continue-wrapper` 时**自动待命**（不注入自己的按钮 + 面板红字提示），避免两套按钮打架；可勾选「忽略旧脚本冲突」放行
- 探测升级为强/弱/无三档：强信号（`blueprint_controller` 等）→ 用真实变量名填 `{var}`；弱信号（「剧情节点」「剧本」「蓝图」等泛词）→ 默认仍显示、措辞回落「剧情蓝图」（沿用旧脚本行为，可关）；无信号 → 只显示「续写」
  - 实测本机 **92 张卡**：强信号 2 张（`翡翠湾度假岛→剧本控制器`、`装甲核心→剧情控制器`）、弱信号 49 张（含用户正在测的「列车求生」）、无信号 41 张；按默认配置 **51 张显示推进按钮 / 41 张只显示续写**
- 新增通用安装器 `scripts/ext-install.mjs`（`extensions/` 下所有扩展一键同步到 ST 数据目录，支持 `--dry-run` / `--check` / `--list` / 指定扩展名），`scripts/compat-install.mjs` 改为它的薄封装（旧命令保持可用）
- 新增夹具 `scripts/plot-pilot-test.mjs`：**46 项断言全通过**，并顺带扫描本机角色卡输出真实探测分布（不联网、只读）
- 公开 API：`window.PlotPilot.{cfg,detection,detect,recheck,continue,advance,sendText,setConfig,cardName}`，后续要加「自动连发/定时推进/别的按钮」都从这里接
- 文档：`extensions/plot-pilot/README.md`（安装、面板逐项说明、冲突处理、API、边界）；`tavern-scripts/continue-button-all-cards.js` 标注为已被本扩展取代
- 安装位置：`D:/AI/SillyTavern/Data/default-user/extensions/plot-pilot/`（未改 ST 本体与角色卡；旧的酒馆助手脚本需要用户自己去停用，未擅自改动）
## v1.34.1 (2026-09-20) — 扩展能自动给出「必更字段清单」并核对覆盖度（0.2.1）
- 回答用户提问「插件能不能做到」：**能，已实现**
- 新增 `extractRequiredFields()`：从角色卡世界书的 `[mvu_update]变量更新规则` 自动解析必更字段与其 `check` 条件（支持 `${A|B}` 模板组递归展开，如 `外貌.${发型|妆容|表情}` → 3 条）
  - 实测本卡抽出 **12 条**：系统.日期/时间/地点/天气/剧情天数、林婉婷/陈慧兰 的位置/外貌×3/穿搭/心情 等
- `buildTailReminder()` 增加「本轮必须更新的字段（缺一项都算失败）」段落，随 depth 0 注入 → 治"模型只更一半"
- 新增 `patchCoverage()`：解析模型写出的 `<UpdateVariable>` 块里的 JSONPatch 路径，统计覆盖/缺失，显示在扩展面板与日志（如「上轮覆盖 2/12（缺 系统.日期、系统.地点…）」）
- 实测用户最新一轮：**覆盖 1/12**（只更了 `系统.时间`），与其反馈"地点和时间不更新"完全吻合
- 夹具断言扩到 **37 项**（新增必更字段抽取 / 模板展开 / 覆盖度 / 提醒 5 项），实测 37/37 通过；扩展版本 0.2.1
- 边界：插件只能"提醒 + 核对"，不能替模型生成数据；若本地 12B 模型遵从度不足，仍建议换模型或改回「额外模型解析 + deepseek-flash」
## v1.34.0 (2026-09-20) — 自动规范化历史楼层，消除「双状态栏」（扩展 0.2.0）
- 现象：同一条消息出现**两个状态栏**（一个正常、一个错乱）。原因：消息里**两种占位符形态并存** ——
  模型写的成对块 `<StatusPlaceHolderImpl>…</StatusPlaceHolderImpl>`（含字段）被另一条渲染路径当成 HTML 渲染出第二个面板，而卡的正则渲染的是自闭合 `<StatusPlaceHolderImpl/>`
- 新增 **`normalizeRecent`**：扩展加载时与切换聊天时，自动扫描**最近 N 楼**（默认 5，可调/可关 `scanRecent`）并做形态规范化 + 去重
  → 历史消息不必再手点「自检当前楼层」，打开聊天即自动修好
- 仍保留「自检当前楼层」按钮作为手动兜底；夹具断言 32/32 通过；扩展版本 0.2.0
- 观察：用户反馈「正常的状态栏部分数据更新了」→ 数据链路已在部分场景工作（待 MVU 解析模型名改为 `deepseek-flash` 后应稳定）
## v1.33.10 (2026-09-20) — 修「状态栏又出问题」：占位符形态统一（扩展 0.1.9）
- 现象：最新一条消息尾部出现 `…</StatusPlaceHolderImpl>` **加** `<StatusPlaceHolderImpl/>` 两种形态并存 → 成对块内容裸露成文本、面板错乱
- 原因（我的回归）：v0.1.8 的 depth 0 提醒写成「`<Tag> … </Tag>` **或** `<Tag/>`」→ 模型两种都写了
- 修复：
  - **形态探测**：从卡的正则脚本判断它需要自闭合（`<Tag/>`）还是成对（`<Tag>…</Tag>`）形态，提醒里**只写那一种**
  - **自动规范化**：卡要自闭合时，把模型写出的成对块折叠成 `<Tag/>`（卡不使用块内内容），并自动去重
  - 修掉"已有自闭合占位符仍去补 `</Tag>`"的冗余动作
  - 正则里转义的 `\/` 先还原再判断，避免成对形态漏检
- 夹具断言扩到 **32 项**（新增形态探测 / 规范化 / 去重 5 项），实测 32/32 通过；扩展版本 0.1.9
- 使用提示：刷新后点面板里的「自检当前楼层」，会把已存在的那条消息也规范化（成对块折叠、只留一个占位符）
## v1.33.9 (2026-09-20) — 新增「通用版继续按钮」酒馆助手脚本（适配所有角色卡）
- 背景：用户把某张角色卡自带的「继续按钮」脚本拖到全局使用，其中「推进剧本步骤」会发送该卡专有指令（`请根据当前blueprint_controller…`），在别的卡上无意义
- 新增 `tavern-scripts/continue-button-all-cards.js`（+ README）：
  - **自动探测**当前卡的剧本系统（世界书/字段/扩展脚本里的 `blueprint_controller`、`blueprint`、`剧本控制器`、`推进至下一个 step` 等），探测到真实变量名即填入文案
  - **按卡显示/隐藏**「推进剧本步骤」按钮：有剧本系统才显示；没有则默认隐藏（可用 `showAdvanceWhenUnknown` 强制显示并走通用文案）
  - **发送更稳**：发送前等待发送按钮可用（生成中最多等 2s）、防连点；DOM 监听改节流（300ms）且仅在按钮缺失时注入
  - 配置入口 `window.YDContinue.{cfg,setConfig,recheck}`，存 localStorage
- 实测（本机 93 张卡）：**有剧本系统 22 张 / 无 71 张**，即 71 张卡上按钮自动隐藏、不会发出无意义指令
- 说明：原「继续按钮」脚本属于用户的酒馆助手脚本，未改动；本项只新增通用版与文档
## v1.33.8 (2026-09-20) — 注入变量块真实格式，治「数据不更新」（扩展 0.1.8）
- 实测确认：状态栏已能正常显示，靠的是扩展补的锚点（面板日志 `anchor-injected StatusPlaceHolderImpl`）；但每轮都记 `data-missing UpdateVariable` → **模型从不输出变量更新块** → 数据库脚本拿不到新数据
- 核实 ST 源码：**卡的内嵌世界书会被自动注入**（`world-info.js:5579/5625/5634`），规则模型看得到 → 属"模型不照做"，不是"规则没给到"
- 核实 settings.json：**`charLore` 出现 0 次** → 那份独立世界书 `worlds/<卡名>.json` 从未被绑定（内嵌那份够用，不影响，但说明绑定机制没生效）
- 新增 `extractVarSpec()`：从角色卡世界书条目里抽出 `format:` 段（含 `<UpdateVariable>` / `<Analysis>` / `_.set(...)` 示例，≤600 字），随 depth 0 提醒一起注入 → 模型不必猜语法
- 夹具断言扩到 **27 项**（新增格式抽取与注入 3 项），实测 27/27 通过；扩展版本 0.1.8
## v1.33.7 (2026-09-20) — 适配「继续按钮」续写场景（扩展 0.1.7）
- 背景：用户重度依赖「继续 / 推进脚本步骤」按钮推进剧情，而续写是**在同一条消息尾部追加**，会带来两类破坏：重复占位符、尾部结构块被撕坏
- 适配 1：**续写后重新守护** —— 原实现每条消息只守护一次（`handled` 集合），续写改写同一消息后不再处理；改为按**文本比对**（`lastSeen` Map），文本变了就再守护一次
- 适配 2：**重复自闭合锚点自动合并** —— 续写常追加出第二个 `<StatusPlaceHolderImpl/>`，新增 `dedupeSelfClosingAnchors()` 只保留最后一个；仅对自闭合占位符生效，成对块不动；面板新增「重复锚点合并」计数
- 夹具断言扩到 **24 项**（新增去重 2 项），实测 24/24 通过；扩展版本 0.1.7
- 说明：继续按钮脚本本身**未做任何改动**（不在本项目内）；本轮仅改 `extensions/card-compat/`
## v1.33.6 (2026-09-20) — 扩展面板字号改为跟随 ST 并可放大（0.1.6）
- 修复：`card-compat` 面板此前把字号写死为 12px，比 ST 界面字号小，看着费劲
- 改为**相对字号**：面板继承 ST 界面字号（`font-size: inherit`），日志与统计用 `0.92~0.95em`；并新增「面板字号」选择：跟随 ST（默认）/ 大（1.15）/ 更大（1.3），通过 CSS 变量 `--cc-font` 生效
- 顺带整章：选项行距、滑条间距、按钮 `min-width` 一并微调
- 说明：只要 ST 的界面字号变大（ST 设置→界面→字号，套壳已把上限从 1.5 提到 2.0），本面板会跟着变大
## v1.33.5 (2026-09-20) — 新增「生成前结尾结构块提醒」注入（扩展 0.1.5）
- 背景实测：该卡连续 4 轮消息里 `<UpdateVariable>` 均缺失（`card-compat` 面板记录 `data-missing UpdateVariable` ×4）→ 状态栏即使出现也没有数据
- 新增：用 ST 官方 API `setExtensionPrompt(key, text, IN_CHAT, depth 0, …)` 在**生成前**注入提醒，让模型最后看到"必须输出当前角色卡要求的结构块"
- 提醒文本由 `buildTailReminder(profile)` 从**当前角色卡**自动推导标签（变量块 + 锚点），**不点名其他卡的标签**，避免串卡；无锚点/无数据块的卡不注入
- 面板新增开关「生成前注入结尾结构块提醒（推荐开）」，默认开；不涉及任何字数限制
- 夹具断言扩到 **22 项**（新增提醒文本 4 项），实测 22/22 通过；扩展版本 0.1.5
## v1.33.4 (2026-09-20) — 锚点判定改为按替换内容 + 面板排版修复（扩展 0.1.4）
- 修复 `card-compat` 从不补锚点的问题：旧逻辑只要脚本名含"隐藏"就把该标签列为不可补；实测该卡里 `<StatusPlaceHolderImpl/>` 同时被「渲染脚本」（大 HTML 替换）与「对AI隐藏状态栏」（空替换）盯上，导致永远不补
- 新判定：**按替换内容分类** —— 有非空替换 → 渲染脚本（标签可补）；明确空串 → 剥除脚本（标签不补）；未提供替换内容 → 退回按脚本名判断；同一标签两者并存时以渲染为准
- 修复设置面板排版：按钮竖排（`width:auto; min-width:120px; white-space:nowrap`）、选项行挤压（checkbox_label flex + 行距）、滑条间距
- 夹具断言扩到 **18 项**（新增"只有剥除脚本不补"与"剥除+渲染并存仍补"两个用例），实测 18/18 通过
- 说明：仅改 `extensions/card-compat/`；ST 本体、角色卡、原预设未动
## v1.33.3 (2026-09-20) — 取消字数限制，改用优先级保证结尾块
- 按用户要求**移除预设变体里的 `max_tokens` 上限**（回到"不限长度"），并把尾块协议里"接近上限就压缩正文"等字数相关表述删掉——生成长度本就每轮不同，不该用上限去修结构问题
- 新增预设条目 `🧷结尾完整性（最高优先级）`：注入 depth 0 且 `forbid_overrides: true`，只声明"回复末尾必须完整输出**当前角色卡要求的结构块**（变量更新块/状态栏块），标签与格式一律以角色卡世界书为准，不得省略、不得改写、不得使用其他角色卡的标签、必须成对闭合"
- 背景：实测该卡内嵌世界书 #13 明确要求"`<UpdateVariable>` 必须放在回复末尾、不可省略"，而最近所有消息里该块均缺失；此前误用 `max_tokens=1800` 是错误方向（正文 3000-4000 字即被截断），已回退
- 影响文件：仅 `Izumi 0828.optimized.json` 与 `Izumi 0828.tailfree.json` 两个副本；ST 本体、角色卡、原预设未动
## v1.33.2 (2026-09-20) — 修串卡标签：预设示例移除 + 扩展 0.1.2
- 修复：预设结尾协议里把 `<status!>` 当示例列出，导致模型在别的角色卡聊天里照抄该标签（实测出现 `</status!` 缺 `>` 的破损标签）→ 去掉全部标签示例，改为"一律以当前角色卡规定的标签为准，禁止使用其他角色卡的标签"
- `extensions/card-compat` 0.1.2：新增畸形结束标签修复（`</Tag` 缺 `>` → `</Tag>`，仅对当前卡已知标签生效）；新增「串卡标签」检测计数（只报告、不改内容）
- 夹具断言扩到 16 项（新增畸形闭合修复 / 串卡检出 / 自家标签不误报），实测 16/16 通过
- 说明：预设两个变体 `Izumi 0828.optimized.json` 与 `Izumi 0828.tailfree.json` 同步更新；ST 本体、角色卡、原预设均未改动
## v1.33.1 (2026-09-20) — 修复 card-compat 在流式模式下完全不执行
- 根因：ST 的 `saveReply({ fromStreaming: true })` 用 `!fromStreaming` 跳过 `MESSAGE_RECEIVED`，而优化预设默认开启了流式 → 守护从未触发
- 修复：`card-compat` v0.1.1 改挂 `GENERATION_ENDED`（流式结束）并在修正文本后调用 `updateMessageBlock(..., { rerenderMessage: true })` 重新渲染，保证卡片正则作用于修正后的文本
- 变更：「缺锚点时补锚点」默认改为开（仅对卡自己定义过、且不在隐藏白名单的锚点）
- 新增：设置面板「自检当前楼层」按钮 + 「未接管」计数（渲染后校验卡脚本是否真的消费了标签）
## v1.33.0 (2026-09-20) — 角色卡状态栏兼容：能力档案 + ST 扩展（第一阶段）
- 新增 `scripts/compat-scan.mjs`：角色卡/预设「能力档案」静态扫描器（卡内正则、酒馆助手脚本、世界书、全局与按预设作用域），实测扫描 93 张卡
  - 档案结果：带正则 80 张 / 带助手脚本 62 张 / 锚点来自正则 45、来自助手 28、无锚点 20；数据块来自正则 42、助手 8、无 43；风险卡 9 张（只有隐藏脚本、无锚点，禁止补锚点）
- 新增 `extensions/card-compat/`：ST 扩展源码（随仓库版本管理，不入 ST 本体）
  - 锚点守护：从卡的正则脚本推导锚点标签族（`<StatusPlaceHolderImpl/>`、`<StatusBar>`、`<status!>` 等），缺失按设置补、未闭合自动补结束标签
  - 数据块守护：`<UpdateVariable>` 等数据块**绝不改内容**，只在未闭合时补闭合；缺失只报警、不伪造
  - 数据新鲜度：对比连续两轮的第N天/日期/时刻/地点/天气，全程一致时提示"数据疑似未更新"
  - 消息区字号：`zoom` 缩放 + 字号下限 `max(Npx,1em)`（卡内写死 px 也能放大）
  - 运行时机：`MESSAGE_RECEIVED`（早于渲染与卡正则），只补标签、不动正文与数据