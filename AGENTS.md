# sillytavern-shell 项目专属规则（AGENTS.md）

> 生效 2026-09-25 ｜ 依据：用户 2026-09-25 直接下达（「像这种就不出安装包了，后面不要问我了，此项目专属规则，非特殊项不需要询问我，不能动本体，但可以通过插件，仅限于此项目，只需要告诉我你做了什么，为什么这样做」）
>
> **优先级**：系统指令 ＞ 开发者指令 ＞ 用户即时指令 ＞ 本文件 ＞ `~/.dsh/AGENTS.md`（全局）。
> 本文件与全局规则冲突时**以本文件为准**（全局 AGENTS.md 自己声明项目级可更具体），并在汇报里写明冲突点。
> 本文件只在 `sillytavern-shell` 项目内生效，**不适用于工作区其他项目**。

---

## 一、自主执行（默认不询问）

本项目的日常改动**直接做、不提问**：改代码、调配置、升版本。

### 1.1 载体：只许改本体，不许动本体

**允许（无需询问）**

- 修改仓库根目录的本体源码：`index.js` / `preload.js` / `shell.js` / `shell.html` / `shell.css` / `i18n*.js` / `webview-preload.cjs` / `prompt*`
- 修改与运行行为相关的仓库内文件：`lib/**` / `scripts/**` / `assets/**` / `.github/workflows/**` / `vendor/**` / `tavern-scripts/**`
- 修改文档：`README.md` / `CHANGELOG.md` / `docs/**` / `extensions/**`
- 提交到 `main`

**禁止（必须交用户决定）**

- 删除或整目录重建 `extensions/<id>/`（扩展目录的结构性替换）
- 改发布配置或工具链：`electron-builder-lite.json` / 换构建器 / 改依赖大版本
- 删除用户数据目录、`D:\AI\SillyTavern\Data/**` 下任何内容
- 发布新的安装包 / 打新 tag（见 1.2）

> 一句话：**本体可改不可删，扩展可自由增改不可整目录删除。**

### 1.2 发包闸门（本项目最常被问到的一条）

**只有「本体」有变化才可能发包**；只改扩展时**一律不打 tag、不发版**。

判据（提交前自检）：本轮 diff 是否触及本体文件？

```
git diff --name-only <本轮起始>..HEAD | Select-String -Pattern '^(index\.js|preload\.js|shell\.|i18n|webview-preload|prompt|lib/|scripts/|assets/|electron-builder-lite\.json|package\.json|\.github/)'
```

- **有命中** → 属于本体变更，可以发包（**发不发仍由用户拍板**，不是自动）
- **无命中**（只有 `extensions/**`、`docs/**`、`CHANGELOG.md`、`README.md`）→ **不发包、不打 tag**，汇报里写明「本轮只改扩展，已通过在线通道生效」

理由：扩展有独立在线通道（推 `main` + `extensions/index.json` 即生效），用户装的是套壳，重发安装包只是多一个内容相同、体量 93 MB 的产物。

### 1.3 版本号规则（沿用全局 5.1，本项目补充细节）

- 单一来源：壳 = `package.json` 的 `version`；扩展 = `extensions/<id>/manifest.json` 的 `version`
- **扩展还有第二处必须同步**：代码里的 `const VERSION = 'x.y.z'`（`extensions/card-compat/index.js`、`extensions/plot-pilot/logic.js`）
- 三位上限：major 无上限；minor ≤ 36；patch 无上限；封顶点 `X.36.6`
- 修 bug → patch +1；加功能 → minor +1 且 patch 归零；破坏性 → major +1 且后两位归零
- 改了代码没升版本号 = 任务未完成，**不得提交**

### 1.4 每轮必做（固定收尾清单）

1. 升版本号（1.3 的三处 + 需要的 `package.json`）
2. `node scripts/ext-index.mjs` 重建清单，`--check` 必须 exit 0
3. 全量夹具：`compat-logic-test` / `ext-deploy-test` / `ext-remote-test` / `ext-manage-test` / `cf-download-test` / `plot-pilot-test` / `toolbox-test`
4. 写 `CHANGELOG.md`（项目根 + 对应的 `extensions/<id>/CHANGELOG.md`）
5. commit（消息用 `-F` 文件，见全局 5.2）→ push `main`
6. 核对线上清单：`extensions/index.json` 是否已是新版本（Contents API，不用 raw 缓存口径）
7. 只改扩展时：把新文件**同步到已装目录** `D:\AI\SillyTavern\Data\default-user\extensions\<id>\` 并逐文件核对 SHA1，然后报告

---

## 二、必须询问用户的「特殊项」（白名单，封闭列举）

**只有下列情况才提问**；不在这张表里的，一律自己决定并继续：

| # | 特殊项 | 为什么必须问 |
|---|---|---|
| 1 | **发包 / 打 tag / 创建 Release** | 用户要拍板（默认不出，见 1.2） |
| 2 | **删除或覆盖非本会话产物**（工作区外的文件、别人的扩展、系统配置） | 不可逆，属工作区外 |
| 3 | **结构性改本体**：删本体文件、换构建器/发布配置、依赖大版本升级 | 影响范围不可准确判断 |
| 4 | **网络/代理/hosts/DNS/防火墙/证书校验开关** | 影响面超出本项目 |
| 5 | **单次操作预计超阈值**：>100k token、>30 分钟、>1 GB 磁盘、>10,000 文件、下载 >500 MB | 消耗用户资源 |
| 6 | 需求本身有歧义，或有多种合理解释且取舍取决于用户偏好 | 无法替他决定 |

**自查句**：想提问之前先确认它是不是上表 1~6 之一 —— **不是就别问**。

---

## 三、汇报格式（本项目精简版）

每轮必须给出**两段**（用户明确要的）：

1. **做了什么** —— 具体动作 + 文件/路径，可核查
2. **为什么这样做** —— 判断依据（实测数据优先）

第三段 **「结果」**（夹具数、版本号、提交号、线上清单）**照常给**，用数据不用形容词。

**只有**命中第二章特殊项时，才追加第四段：`未决（需你决定）` + 选项 + 每项代价 + 我的建议。

---

## 四、本文件自身的优化机制

> 用户要求「优化这条规则」：下面给出可执行的进迭代方式，避免规则变摆设。

### 4.1 每轮自检（三条，答「否」即在汇报里说明）

- 本轮**有没有**向用户提过问题？提的是不是第二章 1~6？
- 本轮**有没有**动本体（删/换结构/删扩展目录）？
- 本轮**有没有**在只改扩展的情况下打 tag / 发包？

### 4.2 违规处置

若发现本轮问了非特殊项，或动了本体：**下一轮内**二选一 —— 改回原状，或把它升级进第二章（连同理由）。不许放着不管。

### 4.3 修订规则

- 修改本文件：追加/重排均可，但必须同步更新文件头的生效日期
- 每次修订在本文末尾「修订记录」加一行：日期 / 改了什么 / 为什么
- 修订本文件**不算代码变更**，不升版本号，但必须在 `CHANGELOG.md` 记一条

---

## 修订记录

| 日期 | 改动 | 原因 |
|---|---|---|
| 2026-09-25 | 建立本文件：自主执行边界（1.1）、发包闸门（1.2）、特殊项白名单（第二章）、汇报精简（第三章）、自检与修订机制（第四章） | 用户 2026-09-25 直接下达「此项目专属规则，非特殊项不需要询问我，不能动本体，但可以通过插件，仅限于此项目，只需要告诉我你做了什么，为什么这样做，优化这条规则」 |
